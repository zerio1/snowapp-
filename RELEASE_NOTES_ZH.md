## v0.2.28

### 新功能

- **终端 GPU（WebGL）渲染**：终端新增 GPU 加速渲染，可在设置中的终端面板开关。
- **用户脚本本地导入**：浏览器设置支持从本地文件导入用户脚本，配置值解析容错，异常数据不再导致失效。

### 改进

- 窗口处于后台时计划任务保持运行，不再随窗口状态中断。
- 内置浏览器 User-Agent 统一为标准桌面 Chrome，移除应用名与 Electron 标识。

### 修复

- 修复数据库恢复流程中的数据丢失隐患，并同步归档表的 schema 列。
- 内置 workflow 服务器补充显示名映射。

## v0.2.27

### 新功能

- **工作流节点自动压缩**：节点 token 达到阈值时自动压缩上下文并继续运行。
- **右侧面板全屏与浮动卡片**：拖动右侧面板超过最大宽度并悬停 500ms 显示遮罩提示，松手进入全屏；全屏聊天可浮动为卡片，浮动头部复用 TopBar 的 TODO 与代码库同步指示器；工作流画布删除（右键/Backspace）统一改为二次确认，确认后显示撤销提示条（Ctrl/Cmd+Z 恢复，10 秒后淡出）；工作流连线增加方向箭头与加粗样式；标签页支持中键快速关闭（Git 固定标签除外）；空聊天快捷卡片与输入框 ModelSelector 随容器宽度渐进收缩，极窄时不再溢出。
- 项目合集成员支持拖拽调整顺序。
- 备忘录列表支持最新/最旧排序切换。

### 修复

- 修复流式输出期间滚动位移误判导致「钉底跟随」被静默关闭的问题，窗口重新可见时自动恢复钉底。
- 修复右侧面板全屏展开/收起过渡时右缘的临时窗口跳动。
- 修复空聊天快捷卡片跨断点收窄时的错位与变形。

## v0.2.26

### 改进

- TopBar 同步指示器改为通过窗口事件打开代码库管理弹窗，解除组件直接耦合。

### 修复

- 修复向上翻页加载历史消息时滚动位置跳动的问题（含新页占位符引起的跳动）。
- 修复聊天底部滚动行为，会话迁移期间视图保持稳定。
- 工具调用卡片图片预览恢复默认光标。

## v0.2.25

### 新功能

- **工作流节点并行执行**：依赖就绪的节点同时启动，每个节点接收全部直接前驱合并的交接文档；节点会话运行期间可继续处理用户消息，完成后转为只读。

### 改进

- 记忆库弹窗支持多选批量删除。
- MCP 传输层握手失败时自动回退 legacy initialize 握手，兼容旧版服务器。
- 文件查看器滚动优化，View/Edit 模式布局调整，横向滚动条固定于视口底部。
- 明确记忆 importance 的注入与检索语义。

### 修复

- 修复「会话+文件」回滚时文件不恢复的问题（恢复前移至 DB 删除之前执行）。
- 修复中文输入法组合输入时快捷键误触发。
- 修复项目无记忆条目时统计失败（SUM 空集返回 NULL）。
- 修复输入框为空时的高度异常。

## v0.2.24

### 新功能

- **工作流失败节点续跑**：新增 `workflow-resume` 工具——节点失败后工作流暂停，经用户确认后可携带可选的 `continuePrompt` 在失败节点的原会话中继续执行（保留完整上下文），成功后剩余节点照常运行。

### 改进

- 回滚确认弹窗新增可选清理项：可一并删除被回滚轮次内保存的项目记忆，默认保留，支持展开查看清单。
- 侧边栏记忆入口显示当前项目的记忆条数徽标，AI 保存/更新/删除记忆后实时刷新。
- `<handoff>` 交接文档渲染为可折叠块；记忆与工作流工具卡片渲染优化。

## v0.2.23

### 新功能

- **持久记忆**：新增 `memory` MCP 服务支持条目级持久记忆——代理可按项目保存/搜索/列出/更新/删除记忆条目，记忆自动注入系统提示；设置页新增记忆管理面板，删除会话时同步清理关联记忆。
- **侧边栏折叠快捷键**：新增左右侧边栏折叠/展开快捷键。

### 改进

- Windows 窗口控制栏合并进入顶栏（TopBar），Plus 按钮重构为可复用组件。
- MCP 添加弹窗的 JSON 编辑器支持语法高亮与容错粘贴解析。
- 成功提交后重置输入框高度。

### 修复

- 删除会话时清理孤儿检查点快照目录。
- 系统设置写入改为在缓存失效后执行（宠物设置改为乐观更新），修复读取旧值的问题。

## v0.2.22

### 新功能

- **Tampermonkey 兼容用户脚本引擎**：内置浏览器支持 Tampermonkey 兼容的用户脚本，浏览器设置页新增「用户脚本」标签，支持列表、启用、编辑器、搜索安装等管理功能。
- **`userscripts` 配置作用域**：config MCP 工具新增 `userscripts` 作用域，AI 可创建/更新/启停/删除脚本并管理 GM\_\* 值（支持 `sourcePath` 与 `raw` 两种格式）。

### 改进

- 聊天滚动重构：加载历史消息时元素锚定滚动恢复、进出迟滞缓冲避免挂载/卸载振荡、占位符 → 内容过渡锁定高度防止滚动塌陷，滚动时滚动条瞬态显示。
- Markdown 渲染器缓存增至 256 项，全块重建时保留滚动位置；视口虚拟化精简。

### 修复

- 修复聊天滚动抖动与冗余布局写入。
- 修复终端浅色主题样式。
- 修复禁用用户脚本的竞态（先落库再刷新缓存）；过期异步快照不再覆盖新快照。

## v0.2.21

### 新功能

- **WorkFlow 持久化与断点续跑**：运行状态与画布跨重启持久化，中断或失败后可续跑且进度不丢失；图拓扑校验收敛到 Rust 层统一实现，归档/删除等操作同步清理运行记录，避免幽灵进度。
- **Skill 编辑**：Skill 设置面板支持直接编辑 Skill 配置，并加固 GitHub Skill 安装。
- **思考阶段统计**：跨所有提供商流测量思考阶段 token 数与耗时并持久化；ThinkingBlock 重构——实时时长/token 读数、完成自动折叠、平滑跟随滚动。
- **`mod+i` 聚焦输入框**：新增 focusInput 快捷键聚焦聊天输入框。

### 改进

- Agent 消息流新增入场动画与折叠/展开高度过渡。
- 聊天滚动条自动隐藏，悬停或滚轮时显示。
- 代码块复制优化，新增复制状态图标。
- 工作流节点只读工具自动放行，无需逐项授权确认。
- 侧边栏多选模式排除运行中的会话。

### 修复

- 修复并行会话检查点归属错误，无关文件不再泄漏进当前会话的回滚列表。
- 修复 Windows 路径大小写导致的检查点清单异常。
- 修复项目切换往返后运行中会话从列表消失（#118）。
- 修复滚动到底部时的「跟随到底部」行为与 favicon 重复绑定监听器。

## v0.2.20

### 新功能

- **WorkFlow 模式**：新增可视化多节点工作流编排模式——AI 通过 `workflow-generate` 工具将需求分解为可执行的无环工作流图，以 React Flow 交互式渲染，支持逐节点编辑 API 配置/模型/提示词后再确认执行；每个节点在自己的会话中运行并接收上一节点的交接文档。节点会话全链路打通：归档/恢复/删除、侧边栏节点树（主会话 → 节点 → 子代理）与画布连线编辑，并与回滚、中止、会话删除等现有系统集成。
- **模型品牌图标**：模型下拉框显示模型品牌图标。
- **用量统计图表**：用量设置新增每日趋势折线图、模型占比环形图与成功率卡片，用量记录支持折叠。

### 改进

- **缩进敏感编辑增强**：缩进敏感文件的模糊编辑支持自动补齐对齐与更宽松的匹配。
- **Plus 菜单重构**：改为紧凑的内联布局。
- **工具卡片优化**：ToolCallNode 懒加载正文渲染，目录重排时显示精确的拖放指示。
- 子代理思考强度不再继承父会话的单次发送覆盖，始终遵循解析后的 API 配置。

### 修复

- 修复单击已有选区时引用弹窗重复弹出。

## v0.2.19

### 新功能

- **用量优化**：通用设置资源占用区新增「优化用量」按钮，对运行时与归档数据库执行 VACUUM + WAL checkpoint、触发 V8 全量 GC 并在 Windows 上收缩进程工作集，回收空闲磁盘与内存并报告回收量。

### 改进

- **存储模块拆分**：Rust 存储单体模块（约 2,400 行）按领域拆分为 16 个子模块，公共 API 保持不变。
- **会话删除清理**：删除会话时清理孤儿上传文件；磁盘回收统一交由「优化用量」处理，避免每次删除都执行 VACUUM 产生额外 IO。

### 修复

- **终端行重叠**：修复 Windows ConPTY 在异常尺寸或高频 resize 时终端行重叠——尺寸钳制最小值并跳过无变化 resize、resize 通知增加 120ms 尾部防抖、容器不可见时冻结自动适配、标签重新激活后强制刷新可见区域。

## v0.2.18

### 改进

- **流式渲染 CPU 优化**：显著降低流式渲染期间主进程与渲染进程的 CPU 占用——Markdown 流式渲染降频至约 10fps（超过 100KB 的超长文本进一步降至 3fps）、流式指标（token 数/耗时）改为每 250ms 合并更新、用户消息栏滚动计算用 rAF 合并、大面毛玻璃移除（仅小元素保留）。
- **恢复后台节流**：移除全局禁用后台定时器节流 / 渲染进程后台化的开关，窗口隐藏或最小化后不再全速运行 rAF 与动画（macOS 上也不再阻止 App Nap）；流式 IPC 事件仍实时到达，后台 AI 会话不受影响。
- **系统设置读取缓存**：Rust 层为系统设置读取新增 200ms TTL 缓存，团队身份轮询、YOLO 模式检查等高频路径不再每次读取都打开数据库连接并解析 schema。

### 修复

- **团队身份轮询死循环**：修复团队摘要 hook 中 identity 依赖导致 load 回调重建、每秒触发 7-28 次身份 IPC 的死循环，此前主进程 CPU 持续高位。

## v0.2.17

### 新功能

- **团队消息附件**：团队消息支持发送图片与文件——输入框可选择或粘贴附件，草稿区预览随消息一并保存；消息气泡显示图片缩略图（点击灯箱预览）与文件下载卡片，删除消息或上传失败时清理对应媒体目录。
- **聊天区划词引用**：选中聊天区文字可一键插入输入框。
- **团队知识 Skill 同步**：团队知识现以 Skill 形式同步。
- **资源占用展示**：通用设置页新增资源占用显示，由 Rust 层支撑。
- **文件系统 MCP 强化**：增强内置文件系统服务的编辑与并发。

### 修复

- **Mermaid 渲染**：新增导入/渲染超时与重试，避免长时间卡住或丢失图表。

## v0.2.16

### 改进

- **团队协作功能开关**：新增团队协作功能开关，默认关闭。
- **模型列表同步**：保存 API 设置编辑后自动同步模型列表。

## v0.2.15

### 新功能

- **团队协作**：新增团队协作面板，支持任务、笔记、评审、动态与成员管理，内置 Markdown 编辑器，数据由 Rust 后端持久化。
- **`@!` Skill 引用**：聊天输入框支持 `@!` 搜索并引用 Skill，用户消息与定时任务中的 Skill chip 同步支持。

### 改进

- **API 设置内联编辑**：模型下拉框可直接打开 API 设置编辑弹窗，编辑弹窗抽离为独立组件。
- **资源管理器快速编辑**：项目资源管理器中双击文件可直接打开快速编辑弹窗。
- **侧边栏布局**：折叠会话区块时，项目区块自动填满剩余高度。
- **图片数据校验**：工具结果与消息中的图片数据增加 magic byte 校验，拒绝非法内容。

### 修复

- **SQLite 锁竞争**：写入引入全局写锁与重试逻辑，避免并发写入时的锁冲突。
- **Windows 环境变量泄漏**：修复 bash / MCP / 终端子进程继承宿主 `PATH` / `NODE_ENV` 的泄漏问题。

## v0.2.14

### 改进

- **集合项目不再重复显示**：已加入集合的项目不再出现在顶层项目列表中，消除集合视图与主列表的重复。
- **项目列表分页**：项目列表分页改用滚动事件驱动，替代 IntersectionObserver，增量加载更可靠。

### 修复

- **归档会话恢复统计**：归档恢复查询缺少运行级 token 统计列，导致恢复的会话丢失运行统计；现已补上相关列。

## v0.2.13

### 新功能

- **项目集合**：新增项目集合功能，支持拖拽操作。

### 改进

- **交互状态管理**：增强推理文本处理与状态管理，每次循环重置迭代流式指标。
- **模糊编辑自动补缩进**：模糊编辑搜索与替换内容自动补齐缺失缩进。
- **UI 优化**：新建会话视图挂载时同步草稿内容并聚焦输入框；工具栏分隔符合并；右面板最大比例调大；表单对话框输入对齐修复。

### 修复

- **Chip 插入位置**：修复外部失焦后 chip 插入位置错误。
- **Mermaid 代码块格式**：移除 Mermaid 示例中多余的代码块标签。
- **图片生成错误信息**：修复图片生成配置检查错误信息中缺失的 `format!()`。

## v0.2.12

### 新功能

- **轻量模式**：新增轻量模式，可禁用重量级 MCP 服务器以节省上下文。
- **可配置发送按键**：聊天输入支持选择 Enter 或 Ctrl+Enter 发送。
- **并行新建会话**：支持并行创建多个新会话，各拥有独立待处理槽位与目标会话位置。
- **按日期删除使用记录**：支持按日期范围删除使用记录。

### 改进

- **只读 MCP 工具并行执行**：无副作用的只读 MCP 工具现在并行执行。
- **Gemini Interactions 协议**：新增 Google Interactions 协议支持，改进工具调用流式、名称解析与工具结果恢复。
- **缩进保护模糊编辑**：模糊编辑对 Python/YAML/Makefile 文件保护行首缩进，缩进变化的编辑会被拒绝。
- **模型选择器 API 设置入口**：模型选择器新增 API 设置入口。
- **其他**：地址输入框右键菜单（剪切/复制/粘贴/全选）、目录右键菜单「在文件管理器中打开」、会话摘要显示平均输出速度、终端按键序列发送与输入等待检测、记录工具取消原因并保留部分 bash 输出。
- **日志脱敏**：API 请求日志中的敏感凭据被脱敏，并修复日志过滤问题。

### 修复

- **Windows 进程等待卡死**：改用轮询 `try_wait` 替代 `Child::wait`，修复 Windows 上进程等待卡死。
- **子代理激活错误处理**：改进子代理激活错误处理，失败会在 UI 中显示。
- **MCP 工具 Schema 兼容性**：工具 Schema 显式声明 `type: object` 与标量类型，兼容 Gemini。

## v0.2.11

### 修复

- **子代理会话查询修复**：子代理会话列表查询（`list_sub_agent_conversations` / `list_sub_agent_conversations_by_parents`）缺少 v0.2.10 新增的运行 token 统计列，导致行映射器越界读取、查询直接失败。现已补上缺失的 `run_input_tokens`、`run_output_tokens`、`run_cache_creation_input_tokens`、`run_cache_read_input_tokens`、`last_run_duration_ms` 五列，子代理会话列表恢复正常并携带运行统计。

## v0.2.10

### 新功能

- **分支会话**：侧边栏会话菜单新增「分支」入口，无需打开会话即可将整个会话复制为新分支。
- **运行摘要栏**：新增会话运行摘要栏，展示持久化的累计 token 与时长统计。
- **全局免审批工具**：支持配置全局无审批工具，并与项目级授权合并生效。
- **编辑自动格式化**：文件编辑后可选自动以 Prettier 格式化，可在设置中开关。

### 改进

- **无操作编辑检测**：检测内容无实际变化的编辑请求，跳过无意义的写入。
- **模型选择器键盘导航**：模型选择器支持键盘操作导航。
- **压缩恢复保留任务消息**：上下文压缩恢复后，用户任务消息得以保留。

### 修复

- **并发锁误报**：分支会话事务改用 `BEGIN IMMEDIATE`，修复并发写入时 `SQLITE_BUSY_SNAPSHOT` 被误报为数据库锁定的问题。

## v0.2.9

### 新功能

- **Token 限定的代码库分块**：新增可配置的 token 上限，防止代码库分块时上下文过大冲击模型。

### 改进

- **隐私遮蔽与 Mermaid 序列化**：增强隐私遮蔽覆盖，更可靠地保护敏感数据；Mermaid 图表现已正确序列化，渲染一致。
- **代码库开关图标区分**：代码库设置开关使用不同图标，视觉区分更清晰。
- **绘图参数下拉框**：将绘图参数下拉框移入自定义弹出层，改善交互体验与布局一致性。

### 修复

- **子代理回滚防护**：修复子代理会话中可能触发回滚操作的问题，防止意外状态变更。
- **首条消息回滚恢复**：修复首条消息回滚后会话输入配置未正确恢复的问题。

## v0.2.8

### 新功能

- **会话上下文拖放**：将侧边栏会话拖入聊天输入框即可作为引用 chip；悬浮 chip 可预览发送时实际注入的上下文（已清理思考与工具细节，并按预算裁剪）。
- **绘图工作台**：右侧面板新增绘图标签页，支持直接生成图片——提示词编辑器带示例、模型能力联动参数（尺寸/格式/思考级别）、参考图、流式预览与历史图库灯箱。
- **LSP 设置面板**：新增图形化 LSP 服务配置管理面板。

### 改进

- **MCP 工具结果限额**：工具结果按模型上下文百分比封顶（`toolResultTokenLimit`，默认 30%），超限截断并附引导信息；图片类结果不受限。可在 API 设置中调整。
- API 配置 JSON 写入时规范化，并附带迁移以合并分歧配置。

### 修复

- 修复原生桥接中 `responsesFastMode` 为 null 时的 `BooleanExpected` 错误。
- 恢复聊天输入框的拖放处理与初始布局。

## v0.2.7

### 新功能

- **检查点批量操作**：支持批量创建、删除与恢复检查点。
- **聊天输入重构**：拆分输入区组件，支持空消息替换，并增强文件、Chip 和模型选择交互。

### 改进

- Responses 请求统一使用解析后的缓存键。
- 升级归档数据结构，稳定空聊天布局。
- 修复终端选择状态并统一主题提交按钮样式。

## v0.2.6

### 新功能

- **WorkTree 模式**：新增隔离 Git 工作流模式——提示词引导基于分支/worktree 的变更，与计划/目标模式互斥，子代理与模式提示词隔离。
- **站点屏蔽**：正则屏蔽规则（`blockedPatterns`）过滤搜索结果，爬取时直接拒绝命中站点并向 AI 返回屏蔽详情；全局规则也通过 app-control 工具与代理设置暴露。
- **来源徽标**：带摘要的 Markdown 链接渲染为站点徽标——悬停显示摘要，点击在应用浏览器中打开来源。
- **定时任务提及**：定时任务提示词现支持文件与图片提及。

### 改进

- 窗口变窄时自动折叠侧边栏。
- 宠物设置整合为单一 JSON 存储。
- 文件工具可与检查点预览并行执行。

### 修复

- 修复检查点预览与子代理变更归属。

## v0.2.5

### 新功能

- **Git 克隆仓库**：新增克隆对话框（URL / 保存位置 / 目标路径预览 / 实时进度），由 Rust 异步子进程执行 `git clone`，完成后自动注册为工作区目录。
- **会话暂停状态**：侧边栏中被用户暂停的流式会话显示暂停图标与警告色。

### 改进

- 视觉请求失败时复用主请求的重试配置（指数退避）；截断且无内容时加倍 `max_tokens` 重试。
- 提交草稿与 AI 生成状态提升到模块级，按仓库路径隔离，切换或折叠后回显。
- **检查点并发重构**：bash 执行期间不再持有执行级锁，跨会话命令并行执行，回滚不被长命令阻塞；新增回滚纪元，检测到回滚覆盖工作树时跳过变更记录，避免跨会话误记。外部 MCP 工具仍使用整树独占锁。

## v0.2.4

### 新功能

- **会话拖放置顶**：会话可在置顶区域与普通列表之间拖拽切换置顶状态，拖拽悬停时高亮提示可放置。
- **CodeLens 远程 SSH 支持**：`find_definition` / `find_references` / `file_outline` 现支持 ssh:// 远程工作区——通过 SSH 读取源文件后在 Rust 阻塞池执行分析。

### 改进

- filesystem / grep / codelens 工具的相对路径统一解析到项目根目录；grep 未指定 path 时默认搜索项目根。
- 回滚条目新增文件类型图标。
- 移除文件查看器的草稿状态指示，清理符号索引与样式死代码。

### 修复

- grep 搜索路径不再静默失败：路径不存在或非文件/目录时返回明确错误。

## v0.2.3

### 新功能

- **更多 IDE 支持**：新增 RustRover、Aqua、DataSpell 的检测与「用...打开」菜单支持。
- **API 提供商搜索**：API 设置中的提供商列表支持搜索过滤。
- **Token 大单位**：Token 数量显示支持 T/Q/Qi 单位。

### 改进

- **检查点增强**：检查点操作提升原子性并本地化；远程检查点通过批量远程 IO 与 SSH 会话复用显著提速。
- **性能优化**：优化面板拖拽与会话切换性能（视口虚拟化）。
- 多选操作按钮增加 Tooltip 并适配窄宽度。

### 修复

- 修复回滚的跨会话竞态条件。
- 修复 Windows 宿主下 WSL 工作目录处理。

## v0.2.2

### 新功能

- **API 配置原子化重命名**：编辑配置名称时以单次事务完成重命名与数据更新，失败时完整回滚。
- **主进程 Web 搜索**：Web 搜索迁移到 Electron 主进程，由 puppeteer 驱动的浏览器（DuckDuckGo/Bing）执行，绕过纯 HTTP 客户端无法处理的 JS 反爬挑战。

### 改进

- API 设置使用新的 `TokenPresetInput` 组件，统一下拉样式并支持键盘操作。

### 修复

- 外部 MCP 子进程树在退出时被完整回收（Windows 使用 Job Object、Unix 使用进程组），修复 MCP 会话关闭后残留 gopls 等后代进程的问题。

## v0.2.1

### 新功能

- **数据库修复**：存储设置中的修复功能可检测并修复损坏的 SQLite 数据库。
- **子代理会话恢复**：子代理会话在应用重启后可恢复并继续。
- **子代理列表与恢复工具**：新增仅主会话可用的 `sub-agents-listSubAgents` / `sub-agents-continue` 工具，可列出并恢复带原始配置与历史的子代理会话。

### 改进

- 敏感命令检查现在同样适用于交互式命令。
- 移除了数据管理（备份 / WebDAV 同步）功能。

## v0.2.0

### 新功能

- **队友通信工具**：子代理现在可以通过 `listTeammates` / `sendMessage` 工具与同一会话中的队友通信。
- **YOLO 模式激活徽标**：YOLO 模式激活时，聊天输入框显示带悬浮提示的徽标。

### 改进

- 优化快捷键设置面板；增强错误边界刷新机制。

### 修复

- 修复多处可选链访问；清理旧的本地绑定。

## v0.1.28

### 新功能

- **数据管理**：新增设置项，支持配置迁移、本地备份、恢复与加密 WebDAV 同步；设置自动保存、备份位置支持目录浏览，UI 已本地化（en/zh-CN/zh-TW）。
- **SSH 工作区检查点**：通过 SFTP 远程文件访问层，检查点的创建/恢复/列表/对比现可在 ssh:// 工作区上工作，回滚对话框带加载状态。
- **对象存储检查点**：检查点重写为自包含的 BLAKE3 内容寻址对象存储，带 mtime/大小指纹缓存——不再依赖 git 状态或 reflink；相同内容只存储一份，非 git 项目也无需全量拷贝。
- **Bash 间接脚本检测**：敏感命令检测现在覆盖间接脚本执行（`bash script.sh` / `source`），按脚本内容匹配并标注来源路径。
- **SSH 路径拦截**：工具调用拦截对项目工作区之外远程路径的访问。
- **输入历史回溯**：聊天输入框支持用 ↑/↓ 键回调用历史消息。
- **子代理待确认状态**：侧边栏显示子代理待确认（提问/工具授权）状态，并可按需置顶父会话。
- **MCP 浏览器面板自动展开**：浏览器命令执行前自动展开已折叠的面板，截图不再拍到空白画面。

### 改进

- 过大的 Rust 原生文件拆分为子模块（浏览器导入、视觉、MCP、存储导出、检查点、git、图库等）；存储模型结构体提取到独立的 `models.rs`。

### 修复

- Git 日志改用 `git log -m --first-parent`，修复合并提交的文件列表与差异为空的问题。

## v0.1.27

### 新功能

- **项目工具授权管理**：新增项目级面板，可查看和删除已授权工具，并支持 `/permissions` 命令（YOLO 模式下禁用）。
- **Git 提交与推送模式**：提交按钮增加下拉菜单，可在「仅提交」与「提交并推送」间切换——选择在重启后保持，推送失败有明确的错误处理。
- **Git 仓库设置**：专属 Git 设置面板，配置扫描深度（默认 1，与 VSCode 一致）、忽略文件夹、变更监听防抖、SSH 轮询间隔、状态变更上限与自动刷新；超大仓库保持响应，本地状态在上限处截断并在 Git 面板给出警告。
- **图库存储位置**：图库目录的切换/重置/迁移移入通用设置存储区块（与 checkpoint/upload 目录相邻，并显示占用大小）；迁移支持取消与自动回滚。
- **独立窗口标签迁移**：分离的浏览器窗口携带所有实例标签的快照，可通过「恢复为标签」菜单项迁移回主窗口右侧面板，保留实例 ID。

### 改进

- **检查点并行化**：工作目录锁升级为 manifest 级粒度的 `RwLock`，同一项目的多个会话可并行处理不同检查点；对象保留遵循 BLAKE3 内容寻址去重，删除检查点无需全量扫描，为常数时间操作。
- **目标模式**：新增「无限制」token 预算选项。
- 消息检查点新增 `checkpoint_ids` 字段，由原生层统一计算。

### 修复

- 元素选择模式不再因 iframe 导航而取消——现在仅在主框架导航时触发。
- 修复 RELEASE_NOTES 中的版本号笔误。

## v0.1.26

### 新功能

- **按消息 ID 截断对话**：对话可从指定消息起截断；修复失败轮次回滚及相关 UI 问题。
- **Shift 修饰键**：键盘快捷键现支持 Shift 修饰键。
- **存储占用显示**：设置中显示存储占用，并优化了检查点快照。

### 修复

- 修复 Git 面板中的差异选择。

## v0.1.25

### 新功能

- **浏览器标签拖放**：将右侧面板的标签拖入聊天输入框——三层页面快照（净化后的正文 / 选中元素 / 视口截图），网页 chip 带上下文菜单（打开页面 / 复制链接 / 移除）、自定义拖拽预览，并支持纯文本拖放。
- **分离标签窗口**：浏览器标签可打开到新的独立窗口。
- **MCP 工具开关**：MCP 设置面板支持按工具启停（全局 + 项目），支持批量/搜索/详情。
- **流中断恢复**：检测流式输出中途中断并自动恢复，提供统一重试策略。
- **定时任务**：SQLite 持久化、每任务覆盖项与预脚本支持。
- **API 设置增强**：面板升级、JSON 树视图与 Anthropic 1M 上下文支持。
- **子代理列表注入**：系统提示动态注入 subAgents 配置（项目级优先）；默认子代理现在正确解析为 agent_general。
- **视觉文本化**：并行多图分析，支持空闲超时 / 取消令牌透传。
- **其他**：侧边栏「新建对话」按钮；右侧面板标签上下文菜单（关闭其他/右侧/左侧）；旧版 .doc/.ppt 提取；toggleWindow/togglePet 快捷键；git 图悬浮提示中的逐提交差异统计。

### 改进

- 统一基础/高级模型路由——会话标题跟随绑定配置。
- 只读 bash 命令跳过检查点并使用目录级锁（并发会话下延迟更低）；只读检测对命令链/控制流/替换更健壮。

### 修复

- 配置切换后重复的上下文压缩。
- 视觉管线中重复的 done 状态事件。
- Windows 绝对路径解析与浏览器导航失败截图。

## v0.1.24

### 新功能

- **会话归档**：对话可归档到独立的冷数据库——归档、恢复与删除会话不再膨胀主数据库。
- **自定义存储目录**：checkpoint 与 upload 目录现在可配置，并支持将已有数据迁移到新位置。
- **侧边栏浏览器多标签**：侧边栏浏览器面板支持多标签，便于多页面工作。
- **API 配置引导条**：未配置 API 时显示引导条，引导用户完成配置。
- **图库灯箱详情**：灯箱详情面板可折叠/展开。
- **宠物审查状态**：宠物轮次追踪重构并新增审查状态。

### 改进

- 聊天输入命令面板的命令搜索增强；imagegen MCP 服务暴露其名称；移除未使用导入。

### 修复

- 聊天输入框的空内容检测不再误触发。

## v0.1.23

### 新功能

- **API 设置增强**：Gemini 配置新增 **Google 搜索**选项（`googleSearch` / `visionGoogleSearch`），注入 Google 搜索工具实现实时网页 grounding；Responses 配置新增 **Fast Mode**（`responsesFastMode`）；表单按请求方式校验字段，非法组合无法提交。
- **逐提交差异查看**：Git 提交图可展开提交列出文件并查看该提交内每个文件的差异；文件上下文菜单可在新的右侧面板标签中打开差异（先加载状态，后异步填充），或复制文件路径。支持本地与 `ssh://` 远程仓库。
- **跨项目通知聚合**：侧边栏聚合**其他项目**中正在流式输出、需关注或已完成的会话，形成按项目的通知区块并带状态徽标（含需关注指示）；点击可跳转到对应项目与对话。
- **图库升级**：相册卡片墙默认视图；按文件名/提示词/模型/提供商模糊搜索；批量模式（移入相册 / 批量删除）；手动多文件导入；拖拽图片到相册卡片进行分类。
- **资源管理器上下文菜单**：在项目资源管理器中右键文件/目录，可在该处打开终端、在系统文件管理器中显示、复制路径，或用已安装的 IDE 打开目录（仅本地条目；SSH 条目保留重命名/删除/打开）。
- **子代理对话视图**：子代理会话头部显示**阶段名称**（激活时截断的提示词）与启动它的主对话；消息上方信息卡片显示代理名称徽标、跳回父会话按钮与完整委托提示词。
- **统一终端 Shell 解析**：一条 shell 解析链路同时服务 Git 面板（WSL 场景）与集成终端；非空 `shellPath` 在保存时校验，`terminal-open` 拒绝缺失的 shell 路径而非静默回退。
- **Bash 执行计时**：每次 `bash` 工具执行都将各阶段耗时（参数解析、敏感检查、远程分发、shell 解析、spawn、首次输出、进程等待、管道排空、总计）连同工具执行 ID 写入应用日志。

### 改进

- **检查点捕获**：git 驱动的 `git worktree` 捕获替代整树遍历/复制，消除串行瓶颈；gitignore 匹配现在遵循子目录规则与 `info/exclude`。
- **主进程 ESM**：`__dirname`/`__filename` 替换为 `import.meta.dirname`/`import.meta.filename`（constants、mainWindow、nativeBridge、插件运行时、发现 worker）。
- **子代理执行链**：优化激活/取消流程并更新文档。
- **导航安全**：渲染层链接/路径点击在 `auxclick` 与 `click` 上均被拦截，主进程阻止应用外的 `will-navigate`——Ctrl/Cmd+点击链接不再重载整个前端并中断进行中的生成。

### 修复

- 消息链接 Ctrl/Cmd+点击不再导航当前窗口（前端重载）并停止流式/生成预览；链接现于应用浏览器面板中打开。
- ImageGen 设置开关因滑块类名不匹配而不可见；类名已对齐。
- Responses API 失败终端事件正确处理（event/stream/retry 路径）。
- **流中重试**：流式响应中途中断（网络 `terminated` / 非用户取消的 `aborted`）现根据已接收内容自动重试——部分工具调用总是重试（截断的工具调用不可用）、短部分文本重试、长部分文本（≥1000 字符）保留为不完整但可用的结果以避免双重 token 成本。用户取消与重试预算耗尽保留原有 keep-partial 行为。覆盖 Chat Completions / Anthropic / Responses / Gemini 流。
- Git 标签是唯一打开的标签时保持可见。
- 对话删除确认对话框统一复用（单个/批量）。

## v0.1.22

### 新功能

- **浏览器凭据导入**：可从 Chrome/Edge/Chromium（macOS Keychain + PBKDF2/AES-128-CBC、Windows DPAPI + AES-256-GCM）与 Firefox（SHA1 迭代 + 3DES-CBC）导入密码与 Cookies，包括 Chrome 133+ Cookie 哈希前缀剥离与 SQLite WAL 锁只读回退。
- **密码保险库**：密码以 AES-256-GCM 加密库存储于磁盘，由系统钥匙串保护；自动填充 IPC 校验发送方 frame 来源，防止跨源读取。
- **Webview 密码助手**：登录表单通过专用 preload（webview-browser 入口）自动填充与自动保存。
- **Webview 弹窗窗口**：`window.open` / `target=_blank` 现在打开真实窗口并保留 opener 关系（Google OAuth 登录所需）。
- **元素选择器**：选择页面元素作为 chip 加入聊天输入框，支持备注与实时样式编辑预览；元素在消息中自动展开为可读描述。
- **浏览器设置面板**：配置起始页、管理密码（搜索/显示隐藏/删除）、从本机浏览器导入密码/Cookies。
- **模型搜索**：模型下拉框支持按模型 ID 或所有者过滤长列表。
- **请求模型持久化**：用户请求的模型现跨 Anthropic、Chat Completions、Gemini、Responses 路径持久保留——提供商回显的带日期戳或别名模型名不再覆盖聊天输入框显示的模型。
- **对话框关闭按钮**：表单对话框头部新增本地化关闭按钮；移除点击遮罩关闭，防止误关。

### 改进

- **聊天滚动**：历史消息异步渲染期间聊天保持钉在底部，会话结束后不再自动吸附；chip 悬浮详情预览；取消添加项目返回父级。
- **指标断点**：容器查询断点按实测宽度重新校准，指标既不提前隐藏也不溢出。
- **Cookie 恢复**：域 Cookies 显式传递 domain 使子域共享登录态；SameSite=None 的非 Secure Cookie 降级以确保可写入。

### 修复

- **流断开重试**：流式响应零输出中途断开时，请求现以指数退避自动重试（3s→30s，最多 5 次），覆盖全部四种协议（chat/anthropic/responses/gemini），并显示可见的重试指示；重试耗尽返回明确错误而非静默空回复。有部分输出的流保持 `incomplete` 以避免内容重复。
- **MCP 握手**：为静默旧 SDK 服务新增 discover 探测超时与 legacy `initialize` 握手回退。
- **敏感命令规则**：预设 `rm` 规则改用词边界，`arm64`、`warm`、`--rm` 等子串不再被误标记；已有预设行自动迁移。
- **ImageGen 编译修复**：修复 `imagegen.rs` 中三个 `json!` 宏缺失的逗号。
- **原生桥接回退**：为回退原生桥补上缺失的 `browserImport*` 方法，非 Rust 运行时给出明确错误。

## v0.1.21

### 新功能

- **AI 代码评审（`/review`）**：以只读提示词评审所选 Git 变更（暂存、未暂存或提交）。提示词以 base64 标记（`@@review:...@@`）渲染为 chip；Rust 展开用于生成与会话标题。新增由 Rust 与 SSH 支撑的 `git:commit-diff` IPC。
- **工作区目录管理**：右键工作区目录可重命名或设为活动目录（内联重命名，Enter/Esc/失焦提交）。目录添加与项目创建改用通用 FormDialog，支持拖放文件夹。
- **IDE 检测**：在 Windows/Linux/macOS 上检测已安装 IDE，并以真实品牌图标（VS Code、Cursor、JetBrains 等，lucide 回退）提供「用...打开」子菜单。
- **SSH 改进**：连接错误分类（网络/超时/认证/sftp/非法/未知）并本地化；支持从 `~/.ssh/config` 导入主机（含 `~`/`%d` 展开）预填连接向导。
- **终端会话身份**：本地进程（一次性命令与持久标签）通过 `SNOW_SESSION_ID`、`TRELLIS_CONTEXT_ID`、`SNOW_CWD`、`SNOW_PLATFORM` 继承 Snow 会话身份，不覆盖已继承值。
- **工具调用渲染**：skill / config / app-control / dbx 工具调用专属卡片；工具名徽标使用基于类别的稳定 lucide 图标与本地化名称（新增 46 个 i18n key）；ImageGen 图库在窄容器中减少列数。
- **Markdown 图片灯箱**：点击 markdown 回复中的图片打开可下载的放大灯箱。
- **数据库恢复**：启动时检测并自动恢复损坏的 SQLite 数据库。
- **MCP**：JSON 草稿编辑重构为单条目 `{name: {...}}` 映射，宽松解析（容器 + 旧格式）；外部工具调用在 "Transport closed" 时通过 legacy initialize 握手重试一次；stdio stderr 转发到应用日志。
- **配置服务**：新增 `~/.snow/ROLE.md` 的 `personalization` 作用域；配置写入前自动备份、成功后清理；`config-delete` 要求显式用户确认。
- **会话隔离**：计划/目标模式严格按会话生效——全局模式设置链已完全移除。
- **代理循环**：仅在循环将继续时执行压缩；`resume_after_compaction` 防止压缩后重复交接；codelens 聚焦符号导航（移除 diagnose 工具与语义分析器）。
- **Hooks**：子代理生命周期钩子绑定到工具卡片；执行结果以结构化操作详情与本地化标签铺满聊天宽度。

### 改进

- **ImageGen**：远程 URL 结果下载并持久化到图库；`n` 接受 1-8（内部扇出）；支持按请求的 `prompts` / `requestImages`；图库迁移分阶段（prepare/chunk/commit）并支持崩溃恢复与回滚。
- **浏览器自动化**：无障碍树快照（`action=ax`）、网络调试（`networkDetails` / `networkState` / `route`）、加密登录态保存/恢复、性能跟踪与新的交互工具（`wait`、`press_key`、`select_option`、`hover`、`upload-file`、back/forward）。
- **浏览器**：MCP 工具名与上游风格统一（`press_key`、`select_option`）；`browser-wait` 新增 `selector`/`selectorGone`；`ref` 定位自动滚动到视口；点击使用真实 50ms 按压间隔；webview 上下文菜单；分离的 DevTools 窗口使用 Snow 图标并纳入生命周期管理。
- Token 悬浮提示显示紧凑 K/M/B 单位；Mermaid 渲染从导入失败中恢复并在下一批重试；对话摘要遵循聊天的思考配置；检查点差异缓存避免重复读取文件。

### 修复

- `safeSend` IPC 避免渲染层 frame 释放竞态；渲染层崩溃后窗口自愈。
- 命令/文件面板打开时 ESC 不再误取消会话。
- 修复 `browser-type` 选择器语法错误；`openSettings` 现接受 `imagegen-settings` / `image-library` 页面。
- 补齐所有语言缺失的 63 个 i18n key。

## v0.1.20

### 新功能

- **浏览器 MCP 工具**：新增 `wait`、`press_key`、`hover`、`navigate_back`、`navigate_forward`、`select_option` 工具。DevTools 扩展 `network_detail` 与 `network_clear` 动作，并支持网络列表可选静态资源过滤。
- **多环境导入发现**：配置与 skill 发现现在跨 WSL 发行版与 SSH 远程主机工作。不支持的 stdio MCP 服务以候选形式呈现并附原因，远程 skill 通过 SFTP 下载，导入设置 UI 显示各环境来源详情。

### 改进

- **Git 图**：提交图显示完整装饰（ref 徽标与带辉光效果的视觉 HEAD 标记）。分支创建下拉重构。
- **Bash 终止安全**：取消与超时分支改用 `biased` select，保证停止请求不丢失；Windows `taskkill` 限时并立即排空流。UI 防止重复 kill IPC 调用并增加渲染层超时看门狗。
- **聊天自动滚动**：滚动状态与几何钉扎解耦，内容快速增长期间保持跟随模式；提前拦截滚轮事件尊重用户意图。
- **max_tokens 处理**：更新 Anthropic 载荷与文件搜索代理中的 `max_tokens` 处理；API 设置增加提示。

## v0.1.19

### 修复

- Responses 请求反复重发请求的问题。

## v0.1.18

### 新功能

- 新增图片生成功能
- 显著降低数据库占用
- 优化首次启动速度
- 定时任务项目隔离
- 输入框支持从外部拖放图片与文件
- 配置文件新增复制功能
- 优化部分组件的 UI 显示

## v0.1.17

### 新功能

- **带参考图的图生图编辑**：附带的图片始终作为图生图编辑的参考图（OpenAI `/images/edits` multipart / Gemini `inlineData`）。主模型不支持视觉时，文本化通道（`api/vision.rs`）为每张图片注入 `[Reference image #N for imagegen-generate: {"path": ..., "mimeType": ...}]` 块——是 upload/ 目录下的相对小路径而非巨型 base64 块——并附带明确引导行，指示模型编辑所附图片而非仅凭描述重新生成。`imagegen-generate` 自行解析 `path` 引用（限制在 upload/ 目录内，拒绝目录穿越；服务端上限 14 张 / 每张 ≤20MB，工具描述引导模型 ≤5 张）。生成卡片上的参考缩略图对内联 base64 与 `path` 引用都显示真实图片（通过新的 `images:resolve-upload-image` IPC 通道从磁盘读取，按会话缓存）。
- **Imagegen 模型能力校验与 400 防护**：`imagegen-generate` 在发送请求前校验模型能力，最常见的提供商 400 错误被预防或自愈：`dall-e-3` 仅文本生图（参考图被拒绝并给出明确的换模型提示）且始终生成恰好 1 张图（`n>1` 被钳制）；`imagen-*` 模型同样仅文本生图。上游 400 响应附带具体修复提示（图片数量 / 图片输入 / 尺寸 / 质量），代理可一步正确重试；工具 `model` 参数描述现在预先说明能力规则。
- **最大并发生成数**：全局 `maxConcurrentImages` 设置（1–8，默认 4，位于设置 → 图片生成与 `imagegen` 配置作用域）限制代理一次请求多张图时的并行生成请求数；其余排队等待，一张完成后立即开始下一张。
- **按会话的输入草稿持久化**：草稿文本（含图片 chip）按会话保存，切回或新建对话时恢复，聊天视图重载时输入不丢失。
- **图库（生成图片管理）**：每张生成的图片持久化到应用安装目录旁的 `image/` 文件夹（安装目录只读时回退到存储目录），并在新 `image_library` 表建立索引（model / provider / prompt / mime / size / dimensions / timestamp）。聊天消息存储小的 `image/...` 路径引用而非巨型 base64 块，数据库停止膨胀。新的**图库**面板（设置侧边栏）提供可筛选网格（比例横/方/竖、时间范围、提供商、模型），支持点击放大灯箱、单图下载与删除——删除图片会物理移除文件、索引行，**并重写引用它的聊天消息**（`content` 与 `raw_json` 都重写）保持对话一致。历史 base64 图片渲染不变；持久化失败时内联 base64 回退仍可用。删除对话时，专用**删除确认弹窗**（单个与批量删除）在所选对话引用图库图片时显示**「同时删除生成的图片」**复选框——勾选后先级联删除这些对话引用的所有图库图片（文件 + 索引行）再删除对话，同时「取消勾选以保留图库中的生成图片」提示让默认保留行为显式化。

### 改进

- **统一删除确认弹窗**：对话删除使用专用弹窗（`ChatDeleteConfirmModal`），单个与批量删除共用——移除条目上下文菜单内的内联确认视图与批量确认栏。弹窗打开时查询所选对话引用的图库图片数，若有则显示**「同时删除生成的图片」**复选框（默认不勾选，附「取消勾选以保留图库中的图片」显式提示）。确认后走单一路径：可选级联图片删除先行，然后删除对话（单个删除保留子代理级联中止/草稿清理；批量保持单个原生事务）。删除受 in-flight 状态保护，对话框不可关闭或重复提交。
- **并行生图的统一图库布局**：单次 `imagegen-generate` 调用的图片共享一行宽度——图库网格按批次设置列数，整体读作一个填满消息宽度的完整区块而非参差不齐的自动填充列：2–4 张共享一行，5–6 张两行三列，7–8 张两行四列（无孤立尾部图）。卡片宽高比跟随真实生成图比例（取批次中位数）：超宽图横跨整行，超高图限制高度，极端比例保持舒适。移除每卡边框与下载/标签装饰，改为干净图片配微妙索引徽标与点击放大；下载操作仅保留在灯箱中。
- **图片生成设置面板（与 API 设置面板对齐）**：通道行不再显示冗余提供商图标，提供商下拉使用共享 `CustomSelect` 组件，内联启用开关拒绝启用没有 API key 或模型的通道（附本地化提示）——与后端规则一致：只有完整配置的通道才向代理暴露生成工具。
- **输入框拖放图片**：输入框现在接受从文件管理器拖入的图片（单个或一次多个），像粘贴一样插入为图片 chip——此前拖放处理器只理解应用内 `application/json` 拖拽载荷（文件/提交/变更标签）并静默忽略外部文件（无拖放光标、无插入）。
- **路径感知的 `@` 文件提及**：`@` 文件面板支持像文件管理器一样浏览文件夹——点击文件夹条目（或 `→` / `Enter`）进入并重写 `@` 查询为相对路径；面包屑栏（工作区根 → 路径段）可跳回，`←` 上一级，直接输入路径（`src/`、`src/renderer/App`）可在目标目录内浏览或过滤。

### 修复

- **Markdown 本地路径图片**：模型在 Markdown 回复中以本地相对路径（`image/...` 图库路径或 `upload/...` 路径）引用生成的图片时，渲染器尝试按相对 URL 加载并显示破图图标。本地路径（反斜杠 / URL 编码变体已归一化，拒绝 `..` 穿越与绝对路径）现在与外部图片一起重写为 `img-proxy://` 协议 URL，主进程直接从磁盘提供——渲染层无 IPC 往返或 data-URL 缓存。
- **i18n 占位符语法**：`settings.imagegenChannelCount` 与 `settings.imageLibraryCount` 使用单花括号 `{count}` 占位符格式，通道数与图库数原样显示而非插值；两者均改用 `{{count}}` 语法。
- 移除 temperature 参数。
- Anthropic 的 thinking.effort 读取后被丢弃。

## v0.1.16

### 新功能

- **原生图片生成 MCP 服务**：新内置 `imagegen` MCP 服务暴露 `imagegen-generate` 工具，双通道支持——OpenAI 兼容 Images API（`/v1/images/generations` 与 `/v1/images/edits`，支持 `b64_json`/`url` 响应格式、质量、输出格式/压缩、每次最多 4 张图与流式部分图片预览）与 Google Gemini（Nano Banana 2+ 模型，经 Interactions API 支持宽高比、最高 4K 图片尺寸、思考级别与 Google 搜索 grounding）。至少一个通道已配置并启用时工具才对模型可见。DB 支撑的 `imagegen` 配置作用域负责通道持久化并带旧格式迁移，流式预览图在会话重载后保留。
- **图片生成设置面板**：图形化多通道管理面板——表格 + 弹窗编辑、内联启用开关（即时保存）、模型能力联动下拉（Gemini 尺寸 × 宽高比组合如 16:9@2K、GPT 图片推荐分辨率表）、别名与废弃/预览徽标，切换模型时自动纠正不支持的尺寸/质量组合。
- **图片生成图库**：聊天中生成的图片以拍立得风格带框照片画廊渲染，层叠阴影与悬浮抬升效果。带磨砂背景的门户灯箱支持全屏查看与下载。流式预览在同一相框内显示并带帧计数。
- **终端 MCP 服务**：新内置 terminal MCP 服务暴露 `open`、`send`、`read`、`resize`、`wait`、`close`、`focus`、`list` 工具，用于交互式终端标签。命令经 Electron IPC 从原生核心桥接到 xterm.js PTY 实例。服务默认禁用，仅在项目显式启用时暴露，可选工具不进模型上下文以节省 token。
- **内置终端体验打磨**：Windows 默认 shell 现在优先 pwsh（PowerShell 7）而非 cmd.exe。终端键绑定包含 Ctrl+C 复制选区、Ctrl+V / Shift+Insert / Ctrl+Shift+C 粘贴或复制、Ctrl+Insert 复制。右键上下文菜单（复制/粘贴/全选/清空）使用主进程剪贴板 IPC。可点击链接在嵌入浏览器中打开，完整 ANSI 16 色调色板应用于浅色/深色主题，终端标签干净退出（code 0）时自动关闭，渲染层导航时终止孤儿 PTY 会话。
- **Bash 分离后台执行**：Bash 工具调用支持分离后台执行并带运行级流指标，长时命令不阻塞代理循环。
- **第三方配置导入**：为 Claude Code、OpenCode、Codex 配置提供统一导入发现与导入工作流。设置页提供图形化导入界面，带事务语义（失败全有或全无）与项目级系统提示导入。
- **插件市场与 Google 主题**：新增插件市场支持与管理 UI，以及新的 Google 主题预设。
- **配置服务全覆盖**：内置 `config` 服务现管理 `~/.snow/` 下所有配置文件——11 个文件作用域（`settings`、含全部 30 个 key 的 `snowcfg`、`proxy`、`app`、`custom-headers`、`system-prompt`、`theme`、`language`、`permissions`、`lsp-config`、`buddy`）加既有 DB 作用域（`subAgents`/`hooks`/`skills`）与新的只读 `logs` 作用域（`~/.snow/log` 列表、带 `limit` 的尾部读取、级别快捷方式）供代理诊断。新 `ValueType::Number` 支持浮点值（如 `theme.diffOpacity`）。
- **项目级 mcpServers 与 sensitiveCommands**：向 `settings.mcpServers` / `settings.sensitiveCommands` 传 `projectId` 现在读写应用数据库中的项目级配置（全量替换语义；与全局规则匹配的敏感命令 id 变为启用覆盖，其余成为项目自定义规则）。
- **深度结构校验**：`settings.codebase`、`custom-header schemes`、`system-prompt prompts` 与 `lsp-config servers` 写入时深度校验（已知字段类型检查、未知字段允许以兼容未来），代理无法破坏嵌套配置。
- **按会话的计划/目标模式隔离**：计划模式、目标模式与目标 token 预算现按会话持久化（NULL = 未设置，跟随全局默认）。会话引用是唯一运行时权威——代理循环、工具执行、压缩与子代理都读取所属会话的模式快照，后台会话不再被另一会话的开关劫持。切换会话不再写入全局设置；计划模式审批仅按会话失效。
- **浏览器 MCP 增强**：新增 `browser-evaluate` 执行页面 JavaScript 并返回 JSON 安全结果，`browser-type` 支持填充、逐键延迟、清空值与可选提交；`browser-devtools` 扩展控制台级别过滤、按 webview 的网络记录，以及经 CDP 的对话框（alert/confirm/prompt）处理，DevTools 关闭后带调试器恢复。
- **多选批量删除对话**：侧边栏会话列表支持多选模式批量删除对话，置顶与会话区块可折叠，长列表更易管理。
- **用户交互工具——手动操作等待**：`user_interaction` 工具现在支持等待用户完成代理自身无法执行的手动操作，用途从澄清问题扩展。
- **Git 面板上下文菜单**：git 图中右键提交行弹出上下文菜单，可复制完整/短哈希或提交消息、展开/折叠提交详情。顶栏项目标签卡片也响应右键（此前被窗口拖拽区域吞掉）。
- **消息栏中的内容标签 chip**：用户消息栏弹出层现在将文件、图片、提交、变更与文本片段标签渲染为内联 chip，而非剥离为纯文本，chip 宽度有上限，并针对 Cream 与 Google 预设优化待处理消息预览主题。
- **标签清理与 Ctrl+点击导航**：右侧面板新增标签清理与 Ctrl+点击文件导航。

### 改进

- **数据库迁移重构**：重构数据库迁移逻辑，带迁移前后钩子；更新 API 配置默认值；增强思考选项支持；更新国际化文本。
- **设计令牌抽取**：将主题令牌、highlight.js 样式、主题设置面板样式与 Cream/Google 预设样式从 `styles.css` 抽取到 `src/renderer/themes/` 下独立文件。`ThemePalette` 在原生、preload 与渲染层新增 `accentColor` 字段。主 `styles.css` 减少约 3,400 行。
- **第三方设置精化**：精化第三方设置 UI 与导入配置格式化；移除废弃测试文件。
- **预设主题更新**：更新 memo、定时任务与 git 提交按钮的预设主题。
- **上下文菜单状态与响应式布局**：新增上下文菜单项禁用/悬浮状态与窄窗口响应式布局调整。
- **文档覆盖**：新增配置文件字段参考（每个文件的字段、类型与敏感标记）、浏览器自动化与代码库/诊断指南（中/英）、数据存储位置与架构概览指南、带分辨率表的图片生成指南，以及聊天/终端/Git 面板指南。`snow-app-docs` skill 与完整配置覆盖对齐。

### 修复

- **子代理计划/目标模式隔离**：子代理对话中禁用计划/目标/目标预算开关，切换不再污染全局模式默认值；终端子代理状态事件在只读标志后立即广播，缩小「输入可见但发送被丢弃」窗口；`isSubAgentFinished` 改用终端状态白名单，`subAgentStatus` 空值保护。另封堵三个会话模式隔离竞态漏洞。
- **浏览器网络记录器启动阻塞**：修复浏览器网络记录器阻塞应用启动的问题。
- **第三方导入加固**：第三方导入改为事务式（失败全有或全无）、加固导入工作流、修正导入发现类型推断，解决插件导入中的 TypeScript 错误。
- **导入的系统提示作用域**：导入的系统提示现在正确限定在项目内而非全局泄漏。
- **阅读器相对链接**：阅读器中的相对 markdown 链接在新阅读器标签中打开，而非触发空白导航。
- **Chat Completions 工具调用**：归一化 Chat Completions 工具调用并修复会话模式处理。

## v0.1.15

### 新功能

- **用户消息栏**：新增右缘悬浮栏快速导航聊天。门户弹出层列出用户消息，分页加载与虚拟化，点击滚动到对应消息。滚动时可见消息在栏中高亮，自定义动画帧补间替代原生平滑滚动以适配流式内容。
- **文本片段 chip**：粘贴超过 2000 字符的文本自动转为可折叠 chip，避免 contenteditable 输入框渲染大文本节点的性能问题。chip 支持悬浮预览、点击编辑弹窗与自动摘要生成。
- **`/changes` 面板**：文件变更统计摘要从消息列表移入 `/changes` 斜杠命令弹窗，带逐文件差异预览。同一文件的重复编辑合并为单条最新记录，重开会话时从持久化历史重新水合统计。子代理变更合并进父会话。
- **非 UTF-8 文件支持**：文件系统读/创建/编辑自动检测编码（BOM + chardetng），写回时保留原始编码与 BOM。CSV 文件按检测到的编码解码。
- **可取消的远程 SSH 工具调用**：SSH 支撑的工具（bash、grep、filesystem）支持按工具取消执行。Rust 注册取消令牌，Electron 将 `tool_execution` ID 映射到关闭 SSH exec 通道的 `AbortController`。会话停止时终止所有运行中的工具执行，而非仅 bash。
- **原生多模态工具图片**：工具结果中的截图现在从 `@@image:@@` 标签拆分，以提供商原生图片内容块（image_url、input_image、inlineData）输出到 Chat Completions、Responses、Anthropic 与 Gemini 载荷，而非把 base64 泄漏到纯文本工具字段。
- **代码库嵌入错误状态**：为代码库嵌入失败新增错误状态与重试流程。
- **Git 图提交悬浮提示**：悬停 git 图提交行显示浮动提示，含完整提交信息（哈希、作者、日期、refs、父提交、消息）。提示在门户中以固定定位渲染，靠近视口边缘自动翻转方向。
- **子代理只读状态**：子代理运行结束（完成、失败或取消）后会话变为只读——输入框替换为状态提示，附返回父会话的快捷方式。子代理排队的用户插入转发到父会话的待处理队列，永不丢失。

### 改进

- **侧边栏列表刷新解耦**：侧边栏会话列表不再随每次消息版本变更重渲染。独立 `conversationListVersion` 仅在显式操作（置顶/删除/重命名/截断）后触发全量重绘，AI 响应使用增量 upsert。内容未变的 upsert 保留原引用避免无意义重渲染。
- **用户消息 ID 同步**：`store_chat_exchange` 现在返回持久化用户消息的 snowflake ID，经所有 API 结果处理器传播。前端在持久化后用真实数据库 ID 替换临时 ID，内存状态与数据库保持同步。
- **待处理消息标签渲染**：待处理排队消息现在正确渲染文件/提交标签与其他 chip。非 macOS 平台合并 `mod` 与 `ctrl` 校验和修复快捷键匹配，macOS 保持精确匹配。
- **聊天输入复制/剪切含 chips**：从聊天输入框复制或剪切选区时，chip 内容通过自定义剪贴板 MIME 类型（`application/x-snow-chat-chips`）序列化，应用内粘贴可完整恢复 chip。同时写入纯文本与 HTML 格式供外部使用。
- **自动更新代理同步**：更新检查与下载前将代理配置同步到 electron-updater 的分区会话——更新器使用独立会话，不继承 `defaultSession` 代理设置。
- **思考内容过滤**：新增 `extract_chat_content` 从 Chat Completions 响应中剥离思考/推理内容，包括内联 `[think]`/`<thinking>` 标记，针对请求 `reasoning_effort=none` 仍返回思考内容的模型。
- **子代理待处理队列转发**：子代理运行结束时，其待处理用户消息队列转发到父会话队列，运行中插入的消息由父循环拾取。

### 修复

- **Mermaid 图片查看器**：修复浅色主题下 Mermaid 图片查看器背景。
- **macOS 托盘活动图标**：托盘活动状态图标此前使用忽略 RGB 颜色的模板图，绿点不可见。现在按系统外观预渲染黑/白雪花线模拟模板反色，绿点统一为绿色，并监听 `nativeTheme` 变化。
- **Cream 主题布局间隙**：移除 Cream 主题的应用布局间隙，包括右侧面板全屏时的内边距。
- **搜索框焦点样式**：搜索弹窗输入框改用焦点环替代单独的边框变色。
- **图标资源路径**：更新资源路径处理，打包后图标正常加载。
- **Grep 输出解析**：修复 grep 输出解析按第一个 `:<digits>:` 对拆分，含冒号的匹配内容不再被丢弃。
- **工具解析错误截断**：工具解析错误在 UTF-8 边界截断，防止非法字符序列。
- **CSS 定位锚点**：为 `.main-content` 与 `.chat-content` 增加显式 `position: relative` 锚点，防止主题禁用 `backdrop-filter` 时子元素漂移。

## v0.1.14

### 新功能

- **代理管理的子代理与 Hooks**：内置 `config` MCP 服务新增三个作用域——`subAgents`（创建/更新/删除子代理，经 `projectId` 全局或项目级）、`hooks`（配置全部 9 个生命周期钩子，全局或项目级）与 `skills`（启停/安装/卸载 skill，委托给 skill 管理器）。子代理与钩子配置直接写入应用数据库，与 UI 设置面板完全一致并立即生效。原 `skills-config-*` 工具移除，改用 `config` 服务的 `skills` 作用域。
- **项目级子代理**：`sub_agent_configs` 表新增 `project_id` 列（复合唯一键，既有数据库自动迁移）。子代理设置面板新增全局/项目作用域标签，子代理激活优先解析项目级代理，回退到同 id 全局代理。
- **应用错误边界**：新增应用级错误边界，动态子包加载失败时自动刷新自愈。通过 `sessionStorage` 限制刷新次数，构建产物缺失时防止无限刷新循环。
- **直接子代理交互**：子代理会话不再只读——使用常规 `ChatInput` 直接交互，独立监控 UI 已移除。子代理模型固定为其自身 `advancedModel`，防止父会话误导性模型记忆。
- **可折叠项目区块**：侧边栏项目区块可折叠，展开/折叠状态持久化到 `localStorage`。

### 改进

- **子代理侧边栏重构**：侧边栏子代理列表移入独立面板，使用独立表面背景，避免与父会话选中态视觉冲突。激活子代理自动展开其父会话，删除父会话级联中止所有子代理流并清空聊天区。
- **会话压缩 API 配置**：压缩会话使用会话级 `apiProfile` 而非全局活动配置，与对话实际使用的 API 配置保持一致。
- **项目规则编辑器**：规则编辑器跟随当前活动项目条目，移除项目下拉选择器，规则设置与当前上下文保持同步。
- **本地化时间标签**：聊天时间戳中的星期名本地化（en、zh-CN、zh-TW），向 `formatTimeLabel` 传入 i18n `t` 函数。
- **TokenUsageRing 占位**：API 配置加载期间显示占位环，避免 token 容量满的误报。
- **MCP 与 Skill 设置**：移除 MCP JSON 批量导入功能；JSON 编辑器错误改用 `AutoDismissNotice`。skill 安装面板显示示例仓库地址。
- **简化会话类型**：移除 `conversationType` 状态，精简会话类型管理。

## v0.1.13

### 新功能

- **会话级 API 配置**：每个对话会话记忆自己的 API 提供商与模型选择。新 `apiProfile` 管线经 Rust 路由，带优雅回退、按会话存储绑定与 `Alt+P` 循环切换提供商快捷键。提供商选择器移入模型菜单二级视图，头部更简洁。
- **系统托盘（macOS）**：完整系统托盘支持——模板图标、悬浮统计、隐藏到托盘。活动状态点参数化而非使用应用 logo，修复 16px 缩小/实心块渲染问题。
- **个性化设置**：专属设置页编辑全局与项目级 `ROLE.md` 文件，带优先级说明。全局与项目规则自动组合，支持 SSH 工作区。
- **内置文档系统**：引入内部文档框架与 `snow-app-docs` skill，代理可阅读内置文档并协助 MCP、skills 与 API 配置。
- **MCP 设置 UI 增强**：新增 JSON 编辑模式、批量导入与本地化错误消息。内置 `config` 与 `skills-config` 服务支持 GitHub token 与 codeload 回退安装 skill。
- **文件变更统计面板**：会话显示文件变更统计面板，汇总新增、删除与修改文件。
- **右侧面板上下文菜单**：标签与终端支持右键上下文菜单，含在终端粘贴。
- **Bash 会话上下文注入**：bash 工具执行注入会话上下文环境变量，子进程可获取会话级信息。
- **项目创建与原始 Markdown 切换**：支持直接从 UI 创建项目，笔记编辑提供原始 markdown 切换。
- **子代理只读视图**：子代理面板优化为只读视图，与主会话更清晰分离。

### 改进

- **网络错误处理与重试**：增强网络错误分类，Rust 层指数退避重试。视觉（图片）请求失败包含诊断消息与 base64 有效性检查。
- **Cream 主题**：引入 Cream 主题（原 Anthropic 主题），精化个性化 UI 样式。
- **Git 图与刷新**：新增手动 git 刷新按钮，改进图车道渲染可读性。
- **会话图标选择器**：会话图标 emoji 选择器迁移到上下文菜单，侧边栏更清爽。
- **行尾归一化**：新增 `.gitattributes` 强制 LF 行尾，防止 Windows 上 CRLF 假差异。

### 修复

- **MCP Discover 回退**：现代 `discover` 握手失败时自动回退 legacy `initialize` 流程（issue #19）。
- **计划模式审批持久化**：计划审批状态跨会话切换保留，并随待处理请求一并迁移。
- **SSH 浏览路径历史**：SSH 目录浏览前后导航不再丢失路径历史。
- **macOS 托盘图标渲染**：修复托盘图标在 macOS 上缩小为 16px 并变成实心块的问题。
- **i18n 完整性**：补齐快捷键与提供商下拉标签的中英文缺失翻译。
- **Copilot 评审反馈**：处理 Copilot 针对代码质量与正确性的四条评审意见。

## v0.1.12

### 新功能

- **macOS 无签名更新流程**：为无签名 macOS 构建实现完整更新管线——生成带各架构 SHA-256 校验和的 `latest-mac.json` 清单，Rust 中获取并校验更新后再应用，并回退 ad-hoc 身份签名，无证书构建无需 Developer ID 证书即可自动更新。
- **按需 Bash 子进程取消**：每条 bash 命令流式输出 `tool_execution` ID，UI 显示停止按钮，会话中止/回滚可终止运行中命令的整个进程树。
- **WSL Git 支持**：配置的终端 shell 为 WSL 时，git 命令通过 `wsl.exe` 运行，带正确的参数引用与 UNC 路径转换。

### 改进

- **非 SSE 流重试移入 Rust**：整个 Gemini/Responses 请求+流循环包裹在单一重试循环中，非 SSE 响应（HTTP 200 JSON 错误或空流）在 Rust 层重试而非返回 JS 代理循环。
- **Git 命令卸载到阻塞池**：NAPI git 导出改用 `spawn_blocking`，仓库操作不再阻塞异步运行时。
- **会话历史加载去重**：会话初始历史仍在加载时切走再切回，不再丢弃进行中的结果或发起重复请求——选择共享单一加载 promise 并缓存结果即时复用。
- **会话级工作目录**：工具执行、检查点创建与钩子 cwd 绑定到会话自身目录而非运行时活动目录，切换项目后检查点保持一致。
- **计划模式审批迁移**：计划模式审批从独立 plan-mode 服务迁移到统一 `app-control` 请求审批流程。
- **TODO 面板重构**：复选框多选替换为内联添加与点击循环状态，工作流更干净快速。
- **文件类型图标**：右侧面板标签与差异查看器新增文件类型图标。
- **发行说明自动化**：GitHub Releases 自动从 `RELEASE_NOTES.md` 提取版本对应变更日志，不再依赖常因标签触发构建丢失的手动输入。

### 修复

- **推理条目往返**：新增 `collect_reasoning_items`，`store: false` 时正确跨请求保留推理输出条目，防止多轮对话丢失推理上下文。
