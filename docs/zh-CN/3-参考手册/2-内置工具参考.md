# 2-内置工具参考

Snow App 内置一套 MCP 工具，供 AI Agent 执行文件操作、终端命令、
网络搜索、浏览器自动化等任务。本文列出全部内置服务器与工具。

## 1. 工具命名规则

内置工具全名为 `{服务器id}-{工具名}`，例如 `filesystem-read`。
部分服务器 id 包含 `-`（如 `user-interaction`），解析工具名时按
内置服务器列表做**最长前缀匹配**消除歧义。

## 2. 服务器总览

按注册顺序列出：

| 服务器 id          | 说明                                                                                                                                                                                                                          |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `filesystem`       | 本地文件读写（read / replace_edit / create）                                                                                                                                                                                  |
| `bash`             | 终端命令执行                                                                                                                                                                                                                  |
| `todo`             | 会话任务清单管理                                                                                                                                                                                                              |
| `grep`             | 文件内容搜索（ripgrep）                                                                                                                                                                                                       |
| `websearch`        | 网络搜索与网页抓取                                                                                                                                                                                                            |
| `browser`          | 内置浏览器自动化（嵌入式 Electron 浏览器）                                                                                                                                                                                    |
| `user-interaction` | 向用户提问（阻塞式交互）                                                                                                                                                                                                      |
| `sub-agents`       | 激活子代理独立执行任务                                                                                                                                                                                                        |
| `codebase`         | 代码库语义搜索（嵌入索引；**仅项目启用代码库索引且已建立索引后暴露**）                                                                                                                                                        |
| `codelens`         | 代码诊断与符号定位                                                                                                                                                                                                            |
| `lsp`              | 外部语言服务器诊断与悬停（**仅表中存在启用且已安装的语言服务器时暴露**）                                                                                                                                                      |
| `app-control`      | 应用控制（备忘录 / 模式 / 设置页 / 定时任务 / 项目）                                                                                                                                                                          |
| `config`           | 全局配置读写（文件：settings/snowcfg/proxy/app/custom-headers/system-prompt/theme/language/permissions/buddy/personalization；数据库：subAgents/hooks/imagegen/apiProfiles/lsp-config/userscripts；委托：skills；只读：logs） |
| `terminal`         | 终端自动化（持久 PTY 会话标签页，区别于 bash 的一次性命令）                                                                                                                                                                   |
| `imagegen`         | AI 图像生成与编辑（OpenAI / Gemini 多渠道；**所有渠道均未配置时按需隐藏**）                                                                                                                                                   |
| `skills`           | 技能（Skill）加载与执行（**动态注册**：无已启用技能时从工具列表隐藏）                                                                                                                                                         |
| `memory`           | 项目级持久记忆（跨会话知识库：保存 / 检索 / 浏览 / 更新 / 删除，按当前会话项目隔离）                                                                                                                                          |

## 3. 工具明细

### filesystem

| 工具全名                  | 用途                                        | 关键参数                                                    |
| ------------------------- | ------------------------------------------- | ----------------------------------------------------------- |
| `filesystem-read`         | 读取文件内容（支持文本、图片、Office 文档） | `filePath`、`startLine`、`endLine`                          |
| `filesystem-replace_edit` | 模糊搜索替换编辑                            | `filePath`、`searchContent`、`replaceContent`、`occurrence` |
| `filesystem-create`       | 创建新文件（自动创建父目录）                | `filePath`、`content`、`overwrite`、`encoding`              |

### bash

| 工具全名                | 用途                                 | 关键参数                                                                                        |
| ----------------------- | ------------------------------------ | ----------------------------------------------------------------------------------------------- |
| `bash-terminal-execute` | 执行终端命令（构建、测试、包管理等） | `command`、`description`、`workingDirectory`、`timeout`、`detach`、`isInteractive`、`sessionId` |

> **Shell 来源**：命令始终在「终端设置」中配置的 Shell 内执行；留空时自动
> 检测系统默认终端（Windows：PowerShell → CMD → Git Bash → COMSPEC）。
> 该工具不接受 `shellPath` 参数——如需指定 Shell 打开交互式终端，请使用
> `terminal-open`。

### todo

| 工具全名           | 用途                         | 关键参数                                                         |
| ------------------ | ---------------------------- | ---------------------------------------------------------------- |
| `todo-todo-manage` | 会话任务清单管理（增删改查） | `action`、`content`、`sessionId`、`status`、`todoId`、`parentId` |

### grep

| 工具全名      | 用途                      | 关键参数                                                                |
| ------------- | ------------------------- | ----------------------------------------------------------------------- |
| `grep-search` | 使用 ripgrep 搜索文件内容 | `pattern`、`path`、`fileGlob`、`caseSensitive`、`isRegex`、`maxResults` |

### websearch

| 工具全名                     | 用途                     | 关键参数                                                             |
| ---------------------------- | ------------------------ | -------------------------------------------------------------------- |
| `websearch-websearch-search` | 网络搜索，返回结果列表   | `query`、`maxResults`                                                |
| `websearch-websearch-fetch`  | 抓取并阅读网页全文或图片 | `url`、`maxLength`、`isUserProvided`、`enableAiSummary`、`userQuery` |

### browser

| 工具全名                   | 用途                                                                                                                                                                      | 关键参数                                                                                                                                                                               |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `browser-create`           | 创建嵌入式浏览器实例                                                                                                                                                      | `url`                                                                                                                                                                                  |
| `browser-navigate`         | 导航到指定 URL                                                                                                                                                            | `url`、`timeoutMs`、`instanceId`                                                                                                                                                       |
| `browser-click`            | 以真实鼠标事件点击页面元素（CSS 选择器 / 可见文本 / 无障碍 ref）                                                                                                          | `selector`、`text`、`ref`、`exact`、`instanceId`                                                                                                                                       |
| `browser-evaluate`         | 在页面执行任意 JavaScript 表达式并返回结果                                                                                                                                | `expression`、`instanceId`                                                                                                                                                             |
| `browser-type`             | 向元素输入文本（一次性设值或逐键；支持 ref 定位）                                                                                                                         | `selector`/`text`/`ref`、`value`、`submit`、`delayMs`、`instanceId`                                                                                                                    |
| `browser-wait`             | 等待文本/元素出现或消失，或等待固定时长（SPA 异步渲染必备）                                                                                                               | `text`、`textGone`、`selector`、`selectorGone`、`time`（毫秒，100-30000）、`timeoutMs`、`instanceId`                                                                                   |
| `browser-press_key`        | 按下键盘按键或组合键（Enter/Tab/Escape/方向键，支持 `Ctrl+A` 式组合）                                                                                                     | `key`、`instanceId`                                                                                                                                                                    |
| `browser-select_option`    | 下拉框选择选项（按 value 或 label 匹配；支持精确文本匹配）                                                                                                                | `selector`/`text`、`values`、`exact`、`instanceId`                                                                                                                                     |
| `browser-hover`            | 悬停元素（真实鼠标移动事件，触发 hover 浮层）                                                                                                                             | `selector`/`text`、`exact`、`instanceId`                                                                                                                                               |
| `browser-upload-file`      | 上传文件到 file input（CDP 直接注入，无文件选择器对话框）                                                                                                                 | `selector`/`text`/`ref`、`files`、`instanceId`                                                                                                                                         |
| `browser-back`             | 浏览器历史后退并等待导航完成                                                                                                                                              | `instanceId`                                                                                                                                                                           |
| `browser-forward`          | 浏览器历史前进并等待导航完成                                                                                                                                              | `instanceId`                                                                                                                                                                           |
| `browser-navigate_back`    | 浏览器历史后退（不等待导航）                                                                                                                                              | `instanceId`                                                                                                                                                                           |
| `browser-navigate_forward` | 浏览器历史前进（不等待导航）                                                                                                                                              | `instanceId`                                                                                                                                                                           |
| `browser-screenshot`       | 截取页面为 PNG                                                                                                                                                            | `fullPage`、`instanceId`                                                                                                                                                               |
| `browser-devtools`         | 文本/无障碍树快照（ax，可 ref 回指）/ 性能 trace / 控制台消息 / 网络请求记录与详情 / 离线模拟 / 路由 mock / 登录态加密保存与恢复 / cookie 管理 / 弹窗处理 / 打开 DevTools | `action`、`verbose`、`maxNodes`、`durationMs`、`level`、`filter`、`limit`、`requestId`、`state`、`pattern`、`fileName`、`domain`、`showValues`、`name`、`dialogResponse`、`instanceId` |
| `browser-close`            | 关闭浏览器标签页                                                                                                                                                          | `instanceId`                                                                                                                                                                           |
| `browser-focus`            | 切换到指定标签页                                                                                                                                                          | `instanceId`                                                                                                                                                                           |
| `browser-list`             | 列出所有打开的标签页                                                                                                                                                      | —                                                                                                                                                                                      |

### user-interaction

| 工具全名                           | 用途                                 | 关键参数              |
| ---------------------------------- | ------------------------------------ | --------------------- |
| `user-interaction-askUserQuestion` | 向用户提问（阻塞式交互，需单独调用） | `question`、`options` |

### sub-agents

| 工具全名                   | 用途                                                                                                                      | 关键参数                    |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------- | --------------------------- |
| `sub-agents-activate`      | 激活子代理独立执行任务                                                                                                    | `agentId`、`prompt`         |
| `sub-agents-listTeammates` | 查询当前会话中**正在运行**的子代理（同会话可见，其他会话不暴露）                                                          | 无                          |
| `sub-agents-sendMessage`   | 给同会话仍在运行的队友子代理发消息；以 Pending 消息在对方当前轮次结束时投递，并带发送者身份前缀                           | `conversationId`、`message` |
| `sub-agents-listSubAgents` | 列出当前会话的子代理（含本次应用运行期间已结束的），返回 `status`（running/completed/failed/cancelled）与 `resumable`     | 无                          |
| `sub-agents-continue`      | 向当前会话的子代理发消息并恢复：已结束时按原配置（模型/工具/系统提示词）与完整历史重新激活；仍在运行则排队为 Pending 消息 | `conversationId`、`message` |

`agentId` 必须从主会话系统提示词注入的子代理清单中选择（内置 `agent_general` 为通用兜底）；不确定时可先调用 `config-list`（scope=`subAgents`）查询。

同会话通信受**会话隔离**约束：`listTeammates` 看不到其他会话的子代理，`sendMessage` / `continue` 跨会话调用会被拒绝。应用重启后，子代理会话可恢复并继续（见 [5-配置Hooks与子代理](../2-使用指南/5-配置Hooks与子代理.md)）。

### codebase

| 工具全名          | 用途                                                                           | 关键参数                                    |
| ----------------- | ------------------------------------------------------------------------------ | ------------------------------------------- |
| `codebase-search` | 代码库语义搜索（基于嵌入索引）；**仅在项目启用了代码库索引且已建立索引后暴露** | `query`（必填）、`topN`（默认 10，最大 50） |

### codelens

| 工具全名                   | 用途             | 关键参数                     |
| -------------------------- | ---------------- | ---------------------------- |
| `codelens-find_definition` | 查找符号定义位置 | `filePath`、`line`、`column` |
| `codelens-find_references` | 查找符号引用位置 | `filePath`、`line`、`column` |
| `codelens-file_outline`    | 获取文件符号大纲 | `filePath`                   |

### lsp

外部语言服务器（rust-analyzer / gopls / pyright 等）驱动的诊断、语义导航
与格式化；**仅当 `lsp_server_configs` 表中存在「启用且已安装」的语言服务器时暴露**
（默认不开启——enabled 只表示用户意图，命令不在 PATH 的服务器视为不可用）。
仅支持本地项目（SSH/远程不支持）。

| 工具全名                    | 用途                                                                                            | 关键参数                                                |
| --------------------------- | ----------------------------------------------------------------------------------------------- | ------------------------------------------------------- |
| `lsp-diagnostics`           | 文件诊断（错误/警告/提示，含精确位置与消息）                                                    | `filePath`（或 `filePaths` 批量）                       |
| `lsp-hover`                 | 符号悬停信息（类型签名/文档，Markdown）                                                         | `filePath`、`line`、`column`                            |
| `lsp-definition`            | 符号定义位置（跨文件语义跳转）                                                                  | `filePath`、`line`、`column`                            |
| `lsp-references`            | 全部引用位置 + 单行代码上下文（上限 100）                                                       | `filePath`、`line`、`column`；可选 `includeDeclaration` |
| `lsp-symbols`               | 文件符号大纲（树形 name/kind/detail/range/children）                                            | `filePath`                                              |
| `lsp-rename`                | 语义级重命名（dryRun 默认 true 只返回多文件 edits；false 写盘 + 同步）                          | `filePath`、`line`、`column`、`newName`；可选 `dryRun`  |
| `lsp-type-definition`       | 跳转符号「类型」的定义（如变量 x → 类 SomeClass）                                               | `filePath`、`line`、`column`                            |
| `lsp-implementation`        | 接口/抽象类/trait 的全部实现位置                                                                | `filePath`、`line`、`column`                            |
| `lsp-code-action`           | 代码操作（快速修复/重构菜单；apply=true 应用 edit 类 action，command 类列出待执行）             | `filePath`、`line`、`column`；可选 `only`、`apply`      |
| `lsp-execute-command`       | 执行服务器命令（如 rust-analyzer.applySourceChange；结果识别 WorkspaceEdit → dryRun 预览/应用） | `command`；可选 `arguments`、`filePath`、`dryRun`       |
| `lsp-call-hierarchy`        | 双向调用链（incoming 谁调用它 + outgoing 它调用谁，含调用点上下文，上限各 100）                 | `filePath`、`line`、`column`                            |
| `lsp-type-hierarchy`        | 类型层级（supertypes 父类型链 + subtypes 全部子类型；仅 Go/Java 项目暴露）                      | `filePath`、`line`、`column`                            |
| `lsp-workspace-symbols`     | 跨项目按名模糊搜索符号（语义级，上限 50，跨启用语言合并）                                       | `query`                                                 |
| `lsp-workspace-diagnostics` | 项目级诊断（LSP 3.17 pull，按文件分组，上限 100 文件 × 200 条）                                 | 可选 `maxFiles`                                         |

### app-control

| 工具全名                            | 用途                                        | 关键参数                                                                                   |
| ----------------------------------- | ------------------------------------------- | ------------------------------------------------------------------------------------------ |
| `app-control-createMemo`            | 创建备忘录（笔记）                          | `content`                                                                                  |
| `app-control-listMemos`             | 列出当前项目的备忘录                        | 可选 `status`                                                                              |
| `app-control-getMemo`               | 读取当前项目的单条备忘录                    | `memoId`                                                                                   |
| `app-control-updateMemoStatus`      | 更新当前项目备忘录状态                      | `memoId`、`status`                                                                         |
| `app-control-getBlockedPatterns`    | 读取全局站点拦截正则规则                    | 无                                                                                         |
| `app-control-updateBlockedPatterns` | 添加、删除或替换全局站点拦截规则            | `operation`、`patterns`                                                                    |
| `app-control-setMode`               | 启用/禁用 Plan Mode 或 Goal Mode            | `mode`、`enabled`                                                                          |
| `app-control-openSettings`          | 打开指定设置页                              | `page`                                                                                     |
| `app-control-createScheduledTask`   | 创建定时任务                                | `name`、`prompt`、`schedule`；可选 `apiProfile`、`model`、`basicModel`、`thinkingStrength` |
| `app-control-createProject`         | 创建项目（工作区目录）                      | `name`、`parentPath`                                                                       |
| `app-control-requestApproval`       | 请求用户确认计划摘要（仅 Plan Mode 下暴露） | `planSummary`                                                                              |

备忘录工具只访问当前会话所属项目的备忘录：`listMemos` 可按 `pending` 或 `done` 过滤，`getMemo` 读取完整记录，`updateMemoStatus` 在校验归属后更新状态。

站点拦截工具访问应用全局的 `proxy_browser_settings.blockedPatterns`，不是项目级配置。`getBlockedPatterns` 返回规则数组和数量；`updateBlockedPatterns` 的 `operation` 为 `add`、`remove` 或 `replace`，`patterns` 为规则字符串数组。Renderer 使用 JavaScript `RegExp` 校验规则，`add` 会精确去重并保持顺序，`remove` 按完整字符串删除，`replace` 完整替换。规则会过滤 `websearch` 搜索结果，并在 `websearch-fetch` 命中时拒绝抓取。

`app-control-createScheduledTask` 的模型字段遵循以下规则：

- `model` 固定任务主请求的高级模型；指定 `apiProfile` 但省略 `model` 时，只使用同一 Profile 的 `advancedModel`，不会借用当前会话或其他 Profile 的模型；
- `basicModel` 只覆盖首次会话标题的模型；省略时使用任务会话所绑定 Profile 的 `basicModel`，不会影响主请求；
- `thinkingStrength` 省略或 trim 后为空时不保存快照，任务触发时继承所用 Profile 的最新配置；
- 这些字段是显式覆盖或 Profile 默认值，Snow App 不会根据提示词复杂度自动切换 basic/advanced 模型，两类模型也不互相回退。

`app-control-openSettings` 的 `page` 参数有效值（22 个设置页，与
`native/src/mcp/servers/app_control.rs` 的 `VALID_SETTINGS_PAGES` 一致；
UI 设置面板另有 general/git/pets/remote-control 四个页面未暴露给本工具）：

| page 值                       | 对应设置页                                |
| ----------------------------- | ----------------------------------------- |
| `api-settings`                | API 设置                                  |
| `imagegen-settings`           | 图像生成                                  |
| `image-library`               | 图片库                                    |
| `proxy-browser-settings`      | 代理与浏览器                              |
| `codebase-settings`           | 代码库                                    |
| `system-prompt-settings`      | 系统提示词                                |
| `personalization-settings`    | 个性化                                    |
| `custom-headers-settings`     | 自定义请求头                              |
| `mcp-settings`                | MCP 设置（服务器与工具级启停，全局+项目） |
| `lsp-settings`                | LSP 设置                                  |
| `import-settings`             | 第三方配置导入                            |
| `skills-settings`             | Skills 设置                               |
| `sub-agent-settings`          | 子代理设置                                |
| `sensitive-command-settings`  | 敏感命令                                  |
| `hooks-settings`              | Hooks 设置                                |
| `theme-settings`              | 主题设置                                  |
| `terminal-settings`           | 终端设置                                  |
| `browser-settings`            | 浏览器设置                                |
| `keyboard-shortcuts-settings` | 快捷键                                    |
| `privacy-settings`            | 隐私设置                                  |
| `usage-settings`              | 用量统计                                  |
| `system-logs`                 | 系统日志                                  |

### config

| 工具全名        | 用途                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 关键参数                                 |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- |
| `config-list`   | 列出可管理配置域（settings/snowcfg/proxy/app/custom-headers/system-prompt/theme/language/permissions/lsp-config/buddy/subAgents/hooks/skills/logs/imagegen/personalization/apiProfiles/userscripts）与各域键；传 `scope` 查看单域详情与当前值（敏感键脱敏）；`apiProfiles` 返回全部 API 档案（apiKey/visionApiKey 脱敏，isActive 标出生效档案）；`userscripts` 返回全部油猴脚本元数据（不含源码）+ 安装引导                                                                                                                                                                                                                                                                                                                                                                                                         | `scope`、`projectId`                     |
| `config-get`    | 读取指定域/键的值；`apiKey`/`visionApiKey` 等敏感键强制脱敏；`subAgents`/`hooks`/`apiProfiles` 域直接读应用数据库（`apiProfiles` 的 key 为档案名，不存在返回 null）；`personalization` 域 `key=role` 返回 `~/.snow/ROLE.md` 规则全文（文件不存在时返回 null）；`userscripts` 域 `key=scriptId` 返回元数据 + 完整源码 + GM 值（脚本不存在返回 null）                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `scope`、`key`、`projectId`              |
| `config-set`    | 写入指定域/键（白名单 + 类型校验 + 写前自动备份 + 原子写，**写入成功后清理本次备份**）；`settings.mcpServers` 自动同步应用数据库立即生效（仅服务器级；**工具级启停为 UI-only**，由 MCP 设置面板管理，不写入 settings.json，也不新增 scope/key）；`subAgents`/`hooks`/`imagegen`/`apiProfiles`/`userscripts` 域直接写应用数据库立即生效（同样写前备份、成功后清理）；`apiProfiles` 支持"无密钥建档→用户后补密钥"（空/省略 apiKey 一律保留旧值）、`isActive:true` 切换生效档案（对新会话立即生效，已有会话绑定创建时的档案）；`personalization` 域 `key=role` 整体替换 `~/.snow/ROLE.md`（值必须为字符串，写后下一个对话生效）；`userscripts` 域按 value 字段分发：`{sourcePath}`（推荐，先写文件再安装）/`{raw}` 新建或更新脚本，`{enabled}` 启用/禁用，`{values}`/`{deleteValues}` 读写 GM 值，`key="new"` 表示新建 | `scope`、`key`、`value`、`projectId`     |
| `config-delete` | 删除指定域/键；**破坏性操作，必须携带 `confirmed: true`（仅限调用方先通过 `user-interaction` 的 `askUserQuestion` 获得用户明确同意后才可携带），否则被拒绝**；写前自动备份、成功后清理。**`imagegen` 域的 delete 是清空全部图像生成渠道（不是只删命名键）**；`skills` 域 delete 卸载技能；`logs` 域 delete 删除日志文件；`subAgents`/`hooks`/`apiProfiles`/`userscripts` 域直接删除数据库记录（`apiProfiles` 删除档案后存储层自动保证至少一个生效档案；`userscripts` 删除 `scriptId` 对应的脚本记录与磁盘脚本文件，需 confirmed）；`personalization` 域 delete 删除 `ROLE.md`（恢复默认规则）                                                                                                                                                                                                                       | `scope`、`key`、`confirmed`、`projectId` |

> **`apiProfiles` 模型字段与激活约束**：`advancedModel` 是 Profile 的默认高级模型
> （普通会话仍使用会话选定模型），`basicModel` 供会话标题、AI Commit、`@?`
> 文件搜索和代码库 Agent Review 使用；Snow App 不按提示词复杂度自动切换两者，
> 两类模型也不互相回退。`config-set` 使用 merge 语义；合并后的 `isActive:true`
> 档案（无论新建为 active 还是之后激活）要求 `advancedModel` 与 `basicModel` 在
> trim 后都非空。inactive 档案可作为模型字段不完整的草稿保存，API Key 是否为空
> 不属于这项模型完整性校验。
>
> **安全机制（配置变更保护）**：为避免 AI 误改/误删用户配置，
> config 工具内置以下约束——
>
> - **删除二次确认**：`config-delete` 必须先经用户确认（`askUserQuestion`）
>   并以 `confirmed: true` 调用，未确认一律拒绝；`imagegen` 的 delete 是
>   **全量清空所有渠道**（非单键删除），更需谨慎。
> - **写前备份**：`config-set` / `config-delete` 在写入前自动把当前值备份到
>   `~/.snow/.config-backups/`（文件型与 DB 型 scope 均覆盖），作为写入期间
>   的临时安全网；**写入成功并验证后删除本次备份**，历史/并发残留由
>   每文件最多 10 份的上限清理兜底。
>
> **结构校验**：`settings.codebase`、`custom-headers.schemes`、
> `system-prompt.prompts`、`lsp-config.servers` 在写入时会做**嵌套结构深度
> 校验**（对已知字段逐一检查类型，如 `codebase.embedding.dimensions` 必须是
> 数字），防止 agent 写坏内部字段；未知字段放行以保持前向兼容。
>
> **项目级 settings**：给 `settings` scope 的 `mcpServers` / `sensitiveCommands`
> 传 `projectId` 可读写**项目级**配置（应用数据库，立即生效，与 UI 项目级设置
> 同源）：`mcpServers` 为全量替换（value 为 `{name: {type,url,command,args,env,headers,enabled,timeoutMs}}`）；
> `sensitiveCommands` 为全量替换（value 为数组，元素 `{commandId, pattern, description, enabled}`，
> commandId 匹配全局规则时作为 enabled 覆盖，其余为项目自定义规则）；
> 其余键不支持 projectId（会明确报错）。

支持的配置域：

| 域                | 文件                                          | 主要键                                                                                                                            |
| ----------------- | --------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| `settings`        | `~/.snow/settings.json`                       | `mcpServers`、`codebase`、`sensitiveCommands`、`yoloMode`、`planMode` 等                                                          |
| `snowcfg`         | `~/.snow/config.json`（`snowcfg` 对象）       | `baseUrl`、`apiKey`（敏感）、`advancedModel`、`chatThinking`、`responsesReasoning`、`maxTokens` 等（当前生效档案的 CLI 兼容镜像） |
| `proxy`           | `~/.snow/proxy-config.json`                   | `enabled`、`host`、`port`、`searchEngine`、`browserPath`、`browserDebugPort`                                                      |
| `app`             | `~/.snow/active-profile.json`                 | `activeProfile`（CLI 兼容层；运行时以数据库 `api_configs.is_active` 为准）                                                        |
| `custom-headers`  | `~/.snow/custom-headers.json`                 | `active`、`schemes`（敏感，可能含 Authorization 头）                                                                              |
| `system-prompt`   | `~/.snow/system-prompt.json`                  | `active`、`prompts`（敏感，提示词正文）                                                                                           |
| `theme`           | `~/.snow/theme.json`                          | `theme`、`simpleMode`、`diffOpacity`、`toolDisplayMode`、`thinkDisplayMode`、`subAgentDisplayMode`、`toolIcons`、`customColors`   |
| `language`        | `~/.snow/language.json`                       | `language`                                                                                                                        |
| `permissions`     | `~/.snow/permissions.json`                    | `alwaysApprovedTools`                                                                                                             |
| `lsp-config`      | `~/.snow/lsp-config.json`                     | `schemaVersion`、`servers`                                                                                                        |
| `buddy`           | `~/.snow/buddy.json`                          | `version`、`companion`、`muted`                                                                                                   |
| `personalization` | `~/.snow/ROLE.md`（纯文本 markdown，非 JSON） | `role`（全局规则全文；list 返回长度+预览，get 返回全文，set 整体写入，delete 删除恢复默认）                                       |

> `proxy` scope 不支持 `blockedPatterns`。站点拦截规则实际存储在应用数据库系统设置 `proxy_browser_settings`，请使用代理与浏览器设置面板或专用 `app-control-getBlockedPatterns` / `app-control-updateBlockedPatterns` 工具维护。

数据库型配置域（写入应用 SQLite 数据库，与 UI 设置面板同源，立即生效）：

| 域            | key                                                                                                             | value                                                                                                                                                                                                                                                                                                                                 | 说明                                                                                                                                                                                                                                                                                                                                                                                                                |
| ------------- | --------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `subAgents`   | `agentId`                                                                                                       | `{name, description?, systemPrompt?, toolsJson?, configProfile?, model?}`                                                                                                                                                                                                                                                             | 创建/更新子代理；`toolsJson` 接受 JSON 字符串或工具名数组；内置 `agent_general` 不可修改/删除；不传 `projectId` 为全局，传入则为项目级                                                                                                                                                                                                                                                                              |
| `hooks`       | `hookType`                                                                                                      | `{rules: [{description, matcher?, hooks: [{type, command?, prompt?, content?, timeout?, enabled?}]}]}`                                                                                                                                                                                                                                | 配置生命周期 hook；不传 `projectId` 为全局，传入则为项目级（项目级覆盖全局）                                                                                                                                                                                                                                                                                                                                        |
| `imagegen`    | `openai` / `gemini`（省略 key 返回全部；无 key 时返回 `{channels, maxConcurrentImages, timeoutSecs}` 完整设置） | 渠道字段：`{enabled, baseUrl?, apiKey?, model?, defaultSize?, defaultQuality?, outputFormat?, webSearch?, defaultStream?}`；顶层另有全局字段 `maxConcurrentImages`（最大并发生成数 1-8）与 `timeoutSecs`（生图请求超时，秒，60-3600，默认 300）                                                                                       | 图像生成多渠道设置（应用数据库 `system_settings` 表，与设置面板同源）；`config-set` 部分更新自动合并（未提供的字段保留原值，`maxConcurrentImages` 与 `timeoutSecs` 也会保留，除非显式提供新值）；`apiKey` 读取一律脱敏（如 `sk-e****7890`）；`config-delete` 清空后生图工具从 AI 工具列表隐藏                                                                                                                       |
| `apiProfiles` | `profileName`（档案名）                                                                                         | `{displayName?, baseUrl?, baseUrlMode?, apiKey?, requestMethod?, advancedModel?, basicModel?, supportsVision?, visionBaseUrl?, visionApiKey?, visionRequestMethod?, visionModel?, maxContextTokens?, maxTokens?, streamIdleTimeoutSec?, enableAutoCompress?, autoCompressThreshold?, maxRetries?, retryBaseDelayMs?, isActive?, ...}` | API 档案（应用数据库 `api_configs` 表，与 UI 同源、立即生效）；`config-set` 为 merge 语义（未提供的字段保留现值，新档案用默认值，`configJson` 自动组装）；**空/省略 `apiKey`/`visionApiKey` 一律保留旧值**——支持"无密钥建档→用户后补密钥"；`isActive:true` 切换生效档案（对新会话立即生效，已有会话绑定创建时的档案，子代理会话严格绑定）；`config-delete` 删除档案（需 confirmed，存储层自动保证至少一个生效档案） |
| `userscripts` | `scriptId`（脚本 UUID；`"new"` 表示新建）                                                                       | `{sourcePath: "<绝对路径>"}` 或 `{raw: "<完整源码>"}`（新建/更新，后端读取文件并解析 `// ==UserScript==` 元数据）；`{enabled: bool}`；`{values: {k: v}}` / `{deleteValues: [k]}`                                                                                                                                                      | 油猴兼容用户脚本（应用数据库 `userscripts` / `userscript_values` 表 + `~/.snowapp/browser-script/` 脚本文件，与「设置 → 浏览器设置 → 油猴脚本」同源、立即生效）；`config-get` 返回元数据 + 完整源码 + GM 值；`config-delete` 删除脚本记录与磁盘文件（需 confirmed）；`@name` 与至少一条 `@match`/`@include` 为必填元数据，详见 [油猴脚本](../2-使用指南/22-油猴脚本.md)                                             |

委托型配置域（复用技能管理服务 SkillsConfigService 的实现，存储机制与 UI 一致）：

| 域       | key       | value                                                   | 说明                                                                                                                                                                           |
| -------- | --------- | ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `skills` | `skillId` | `{enabled}` 切换开关 / `{url, location}` 从 GitHub 安装 | 全局开关改写 SKILL.md frontmatter 的 `enable` 字段；项目级开关写数据库 `skill_overrides`；安装/卸载操作 `~/.snow/skills` 目录与 `skills-registry.json`；`projectId` 表示项目级 |

只读日志域（供 agent 自主分析应用异常，无需人工看日志）：

| 域     | key                                                                                                    | 说明                                                                                                                                                                                                                                                                                           |
| ------ | ------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `logs` | 日志文件名（如 `2026-08-03-error.log`）或级别简写（`error`/`warn`/`info`/`debug`，读取今天的对应文件） | `config-list logs` 返回 `~/.snow/log` 下全部日志文件（按日期倒序，含大小/级别）与最近错误文件摘要；`config-get logs` 读取文件尾部内容（可选 `limit` 控制行数，默认 200、最大 2000，环形缓冲防大文件全量加载）；`config-set logs` 只读；`config-delete logs` 仅接受精确文件名删除（防路径穿越） |

> 配置示例见 [2-使用指南/5-配置 Hooks 与子代理](../2-使用指南/5-配置Hooks与子代理.md)。

### terminal

管理右侧面板中的终端标签页（持久交互式 PTY 会话，跨多次调用保持存活），
与 `bash-terminal-execute`（单次一次性命令）互补。省略 `tabId` 时作用于最近聚焦的标签页。

| 工具全名          | 用途                                                                                                              | 关键参数                                                        |
| ----------------- | ----------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- |
| `terminal-open`   | 打开新终端标签页（持久登录 shell 会话；省略 `cwd` 时使用当前项目目录；省略 `shellPath` 时使用终端设置或系统默认） | `cwd`、`shellPath`                                              |
| `terminal-send`   | 向终端 PTY 发送输入（如同用户键入；省略换行时后端自动补 \n 触发执行）                                             | `input`（必填）、`tabId`                                        |
| `terminal-read`   | 读取终端屏幕缓冲区的当前可见文本（ANSI 已剥离；非完整回滚历史）                                                   | `tabId`、`waitMs`（0-60000）                                    |
| `terminal-resize` | 调整终端 PTY 尺寸（同时更新 PTY 进程与显示）                                                                      | `cols`（1-500）、`rows`（1-200）、`tabId`                       |
| `terminal-wait`   | 等待终端进入空闲（静默期无新输出），用于探测长命令是否结束                                                        | `tabId`、`timeoutMs`（≥1000，默认 30000）、`idleMs`（100-5000） |
| `terminal-close`  | 关闭终端标签页并终止其 PTY 进程                                                                                   | `tabId`                                                         |
| `terminal-focus`  | 切换到指定终端标签页（前台激活）                                                                                  | `tabId`（必填）                                                 |
| `terminal-list`   | 列出所有打开的终端标签页（id、标题、工作目录、活动状态）                                                          | —                                                               |

### imagegen

| 工具全名                  | 用途                                                                                                                                                                                                                     | 关键参数                                 |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------- |
| `imagegen-generate`       | 文生图 / 图生图编辑（OpenAI + Gemini Nano Banana 多渠道，独立于对话 API 配置；**所有渠道均未配置时工具从 AI 工具列表隐藏**）                                                                                             | 见下方 20 参数表                         |
| `imagegen-image-describe` | 用视觉模型分析磁盘上的图片并返回结构化描述（布局/配色/字体/间距/组件），用于「读取设计稿 → 理解设计 → 编码还原前端」等工作流；复用主 API 配置的 vision 通道（chat / responses / anthropic / gemini），带 blake3 内容缓存 | `path`（必填）、`prompt`（可选分析重点） |

`imagegen-generate` 完整参数：

| 参数                | 类型           | 说明                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------- | -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `prompt`            | string（必填） | 生成描述；提供参考图时为编辑指令（**一次调用 = 一张图**；需要多张图请并行发起多次调用）                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `prompts`           | array          | **兼容保留，不推荐**：逐请求不同提示词（1-8 项，覆盖 `n`）。生成多张**各不相同**的图片请**并行发起多次调用**（每次一张、各自独立的 `prompt`），不要用本参数在单次调用里塞多个提示词                                                                                                                                                                                                                                                                                                                                                            |
| `images`            | array          | 参考图 `[{data, mimeType}]`（base64，不含 `data:` 前缀；兼容带前缀的 data URL）或 `[{path, mimeType}]`。`path` 可为可信的绝对磁盘路径，或来自 `[Reference image #N for imagegen-generate: ...]` 引用块的安全 `upload/...` 相对路径；相对路径必须以 `upload/` 开头并拒绝 `..` 穿越，服务端会自动读取并编码，无需复制大段 base64。图生图/编辑用；运行时每组上限 **14 张**、单张编码数据约 20 MiB（工具描述建议 AI 单次 ≤5 张，仅为兼容部分服务商更严格的限制）；OpenAI 走 `/images/edits`、Gemini 走 `inlineData`；提供 `requestImages` 时被忽略 |
| `requestImages`     | array          | **兼容保留，不推荐**：逐请求独立参考图组 `[[组1...], [组2...], ...]`（每组的形状同 `images`），组数必须 = 请求数（= `prompts` 长度或 `n`，1-8）。多张不同源图分别编辑/重绘请**并行发起多次调用**（每次一张、各自带独立的 `images`）；未提供时所有请求共用 `images`                                                                                                                                                                                                                                                                             |
| `model`             | string         | 覆盖设置中的模型；OpenAI：`gpt-image-1`/`gpt-image-2`/`dall-e-3`；Gemini：`gemini-3.1-flash-image`（Nano Banana 2）/`gemini-3.1-flash-lite-image`（Lite）/`gemini-3-pro-image`（Pro）/`gemini-2.5-flash-image`（旧版）。**能力规则（不支持的请求会 400）**：`dall-e-3` 仅文生图（不接受参考图）且每次仅生成 1 张；`imagen-*` 同样仅文生图                                                                                                                                                                                                      |
| `provider`          | enum           | `auto`（默认，按配置推导）/ `openai` / `gemini`，后端切换                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `size`              | string         | OpenAI：`1024x1024`、`1024x1536`、`1536x1024` 等；Gemini：`1K`/`2K`/`4K`（imageSize）或 `16:9`、`1:1`、`9:16` 等（aspectRatio）                                                                                                                                                                                                                                                                                                                                                                                                                |
| `quality`           | enum           | `low` / `medium` / `high` / `auto`；OpenAI 仅 gpt-image 系列生效，Gemini 仅接受 `low`/`medium`/`high`（`auto` 被忽略）                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `outputFormat`      | enum           | OpenAI：`png`（默认）/ `jpeg` / `webp`；dall-e 与 Gemini 忽略                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `outputCompression` | number         | OpenAI JPEG/WebP 压缩率 0-100                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `n`                 | number         | **兼容保留，不推荐**：1-8（默认 1）。生成多张图片请**并行发起多次调用**（每次一张），不要调大 `n`；n>1 仅对**同一提示词**拆成 n 个**并发子请求**（每请求 1 张，绕开上游不支持单请求多图），整批返回并各自落库；n>1 时自动关闭流式预览；`dall-e-3` 恒为 1。传 `prompts` 数组时由数组长度决定请求数                                                                                                                                                                                                                                              |
| `personGeneration`  | enum           | Gemini：`dont_allow`（默认）/ `allow_all` / `allow_adult`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `webSearch`         | boolean        | Gemini 联网搜索（Grounding with Google Search），默认取设置                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `stream`            | boolean        | 流式预览（OpenAI `partial_images` SSE / Gemini `streamGenerateContent`），默认取设置；dall-e 与图生图忽略                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `inputFidelity`     | enum           | OpenAI 编辑保真度：`low` / `high` / `auto`（默认）；`gpt-image-2` 不支持（恒为高保真）                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `background`        | enum           | OpenAI：`opaque`（默认）/ `transparent` / `auto`；透明背景需模型支持（`gpt-image-2` 等不支持时自动降级为 `opaque`）                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `moderation`        | enum           | OpenAI：`auto`（默认）/ `low`（更宽松过滤）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `seed`              | number         | 确定性种子，复现结果                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `thinkingLevel`     | enum           | Gemini 3.1 Flash Image：`minimal`（默认，快）/ `high`（质量更高，慢）                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `imageSearch`       | boolean        | Gemini 3.1 Flash Image：Google Image Search 联网参考（需展示搜索建议）                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

> **配置与暴露规则**：渠道需 `enabled` + `apiKey` + `model` 三者齐备才算可用；
> 多个渠道可同时启用，AI 按请求选择（未指定渠道时按渠道顺序取第一个可用渠道，也可传 `provider` 指定）；
> 均未配置时 `imagegen-generate` 从模型工具列表隐藏。
> 不支持的模型能力会在**发送前**自动校验（`dall-e-3`/`imagen-*` 传参考图
> 本地拦截并提示换模型；`dall-e-3` 的 `n` 自动收敛为 1）；仍出现的上游 400
> 会附带具体修复提示（多图数量 / 图像输入 / 尺寸 / 质量）。
> 图形界面配置见 [2-使用指南/9-图像生成](../2-使用指南/9-图像生成.md)；
> 也可通过 `config` 工具的 `imagegen` scope 读写（见上文，apiKey 脱敏）；
> 多图生成通过**并行多次调用**实现（一次调用 = 一张图），受设置中的「最大并发生成数」限制（1-8，默认 4），超出排队、完成一张补一张；`n`/`prompts`/`requestImages` 为兼容保留参数，不推荐使用。
> **Imagen 已弃用**：`imagen-*` 模型 2026-08-17 关停，请用 Nano Banana 系列。

### skills

| 工具全名               | 用途               | 关键参数 |
| ---------------------- | ------------------ | -------- |
| `skills-skill-execute` | 加载并执行指定技能 | `skill`  |

> 注意：frontmatter 中控制开关的字段名是 `enable`（不是 `enabled`）。
> 历史上的 `skills-config-*` 工具（list/setEnabled/installGithub/uninstall）
> 已移除，技能管理统一使用 `config` 服务器的 `skills` scope（见上文）。
>
> `skills` 服务器为**动态注册**：仅当 `~/.snow/skills` 或项目 `.snow/skills`
> 下存在至少一个已启用技能时，`skills-skill-execute` 才会出现在工具列表中；
> 全部技能被禁用/卸载后工具自动隐藏。

### memory

| 工具全名        | 用途                                       | 关键参数                                                               |
| --------------- | ------------------------------------------ | ---------------------------------------------------------------------- |
| `memory-save`   | 保存跨会话的项目记忆（同标题自动合并去重） | `title`、`content`、`kind`、`importance`、`tags`、`sessionId`          |
| `memory-search` | 按关键词检索项目记忆（打分排序）           | `query`、`kind`、`status`、`limit`                                     |
| `memory-list`   | 分页浏览项目记忆                           | `status`、`kind`、`limit`、`offset`                                    |
| `memory-update` | 更新记忆条目（缺省字段保持不变）           | `memoryId`、`title`、`content`、`kind`、`importance`、`status`、`tags` |
| `memory-delete` | 删除单条记忆（**破坏性**，需用户确认）     | `memoryId`、`confirmed`                                                |

> **项目隔离与注入**：所有 memory 工具按当前会话的项目（`directory_id`）
> 隔离读写，模型无法跨项目访问；项目上下文由调用链注入，工具参数不
> 携带 projectId。主会话的系统提示词末尾自动注入 `## Project Memory`
> 章节：`importance >= 3` 的 active 条目按重要性取头部（上限 30 条 /
> 4000 字符），尾部附带当前会话 ID 引导 `memory-save` 传递 `sessionId`
> （记忆溯源锚点）。空记忆库时注入简短引导。子代理不注入该章节。
> 删除会话时，确认弹窗会统计该会话保存的记忆数并让用户选择是否连带
> 删除（默认保留）。
>
> `memory-memory-delete` 为破坏性操作：必须先通过
> `user-interaction-askUserQuestion` 获得用户明确同意，再携带
> `confirmed: true` 重试；不带确认参数的调用会被拒绝。

## 4. 特殊说明

**`app-control-openSettings` 的 `page` 参数合法值：**

| page 值                       | 对应设置页                                |
| ----------------------------- | ----------------------------------------- |
| `api-settings`                | API 设置                                  |
| `imagegen-settings`           | 图像生成                                  |
| `image-library`               | 图像库                                    |
| `proxy-browser-settings`      | 代理与浏览器                              |
| `codebase-settings`           | 代码库设置                                |
| `system-prompt-settings`      | 系统提示词                                |
| `personalization-settings`    | 个性化                                    |
| `custom-headers-settings`     | 自定义请求头                              |
| `mcp-settings`                | MCP 设置（服务器与工具级启停，全局+项目） |
| `import-settings`             | 导入                                      |
| `skills-settings`             | Skills 设置                               |
| `sub-agent-settings`          | 子代理设置                                |
| `sensitive-command-settings`  | 敏感命令                                  |
| `hooks-settings`              | Hooks 设置                                |
| `theme-settings`              | 主题设置                                  |
| `terminal-settings`           | 终端设置                                  |
| `browser-settings`            | 浏览器设置                                |
| `keyboard-shortcuts-settings` | 快捷键                                    |
| `privacy-settings`            | 隐私设置                                  |
| `usage-settings`              | 用量设置                                  |
| `system-logs`                 | 系统日志                                  |

**其他说明：**

- `app-control-requestApproval` 仅在 Plan Mode 启用时暴露；
- `skills-skill-execute` 动态加载 `~/.snow/skills` 与项目 `.snow/skills`
  下已启用的技能。

## 5. 与外部 MCP 工具的区别

外部 MCP 服务器工具由用户在 **MCP 设置** 中添加
（见 [2-使用指南/1-配置 MCP 服务器](../2-使用指南/1-配置MCP服务器.md)），
工具名前缀为服务器名（如 `dbx-search_context`），与内置工具通过
内置服务器列表前缀匹配的方式区分。
